# 測試計畫

[English](TEST-PLAN.md) | [繁體中文](TEST-PLAN-zh-TW.md)

發版前要驗證什麼、在哪一層驗證，以及——在能講清楚的範圍內誠實說明——
哪些至今仍然完全沒有驗證。

之所以分層，是因為各層擋的錯誤種類不同。自動化測試擋的是「原本會擋的防護
不再擋了」；機械檢查擋的是「沒人記得更新版本號」；資安掃描擋的是「某個標頭
不見了」；瀏覽器清單擋的是「必須有人看著螢幕才會發現」的問題。
任何一層都不能取代另一層。

---

## 執行方式

```bash
python3 -m unittest discover -s tests     # 146 項，完全離線
python3 tools/preflight.py                # 11 項發版前機械檢查

# 系統沒有 Flask 的主機
python3 -m pip install --no-index --find-links=offline_packages \
        --target=/tmp/vendor flask requests pyyaml
PYTHONPATH=/tmp/vendor python3 -m unittest discover -s tests
```

測試不需要真的 Wazuh Manager：API 層以 mock 取代，規則目錄是模擬的。

---

## 第一層——自動化測試

`tests/test_web_ui.py` 涵蓋讀取路徑與資安性質；
`tests/test_ui_operations.py` 涵蓋**會改動東西**的路由。

| 範圍 | 驗證內容 |
|---|---|
| 認證 | 對 app 自己的 `url_map` 中**每一條**路由測試未登入的呼叫。清單由 url_map 推導而非手寫，所以日後新增的路由會自動被涵蓋，不必靠人記得補。Session 過期會回傳 `session_expired`。 |
| 安全標頭 | CSP、`X-Content-Type-Options`、`X-Frame-Options`、referrer policy、`Server` 標頭不洩漏版本。 |
| 供應鏈 | 每個 CDN 的 `<script>` 與 `<link>` 都帶 SRI 雜湊。 |
| CSRF | 登入表單會發出並要求 token。 |
| 反射型 XSS | 回填進模板的值都經過轉義。 |
| 輸入驗證 | 代理程式 ID、群組名稱、節點名稱、檔名、主動回應指令與參數。含 `..` 當名稱、shell 特殊字元、超長值。 |
| 破壞性路由 | Queue DB 清除、群組成員異動、升級檔刪除，**只測試被拒絕的路徑**。當清單位置收到字串時，不可被逐字元迭代。 |
| 路徑封閉性 | 群組檔案讀取、節點檔案下載（以關鍵字指定，絕不用路徑）、WPK 刪除、套件 manifest 的目的地。 |
| 設定編輯 | 空的、過大的、格式錯誤的內容都會被拒絕；XML 外部實體不會被解析。 |
| 規則 | 階層、列表、內容搜尋、檔名搜尋，以及 ElementTree 無法解析的損壞規則檔。 |
| 解碼器、CDB 清單 | 列出、讀取，以及 CDB 的寫入路徑。 |
| 記錄測試 | 請求格式與回應解析。 |
| 資產清單 | 跨代理程式的套件查詢。 |
| 叢集 | 跨節點規則集重載；節點設定差異比對。CDB 清單宣告是否送達每個節點：宣告文字本身、peer 寫入路徑、「接受寫入卻沒生效」的節點、重複宣告不會再寫一次、連不到節點時中止並回滾，以及 `local_only` 覆寫。 |
| 規則套件 | Manifest 完整性、每個隨附檔案的 SHA-256、規則 ID 不重複、`dest` 路徑、`packs/INDEX` 一致性、指令碼／排程／代理程式群組的安裝，以及套件內不含任何內部主機或位址。 |
| 前端 | 模板的 JavaScript 能通過 `node --check`。i18n 字典已嵌入且內部一致。 |
| 版本比較 | 代理程式版本比較器，含預先發行版本的情況。 |

**為什麼破壞性路由只測被拒絕的路徑。** 其中好幾條在驗證通過後會呼叫外部指令
或刪除檔案。若測試把它們推到成功路徑，就會真的動到執行測試的那台機器——
而在這個專案裡，那台機器通常是**正式運作中的 Wazuh Manager**。
值得斷言的是防護本身；防護之後的部分，刻意留到第五層由人工驗證。

---

## 第二層——機械式發版前檢查

`tools/preflight.py`，全部都必須通過：

1. 版本號與各處 badge 與 `lib/__init__.py` 一致
2. 兩份 CHANGELOG 都有本版本的條目
3. `github/` 與工作目錄內容一致
4. 對外發佈的內容不含任何內部主機名、位址或郵件帳號
5. 套件 manifest：雜湊、規則 ID、`dest` 路徑、描述
6. `packs/INDEX` 與實際檔案相符
7. `web_ui.py` 內嵌的 i18n 字典是最新的
8. 模板的 JavaScript 可正常解析
9. README 引用的每張截圖都存在
10. 沒有任何隨附的 CDB 清單仍是佔位符
11. 發佈衛生——不含 `wazuh-rules/`、不含殘留封存檔、不含機密

第 10 項之所以存在，是因為曾經發佈過一條 level 15 規則，
它指向的清單裡只有一筆捏造的雜湊。這條規則不可能觸發，而且沒有任何機制說出來。

---

## 第三層——資安掃描

```bash
PYTHONPATH=/tmp/vendor python3 wazuh_agent_mgr.py --web --host 127.0.0.1 --port 5099 &
docker run --rm --network host -v /tmp/zap:/zap/wrk:rw ghcr.io/zaproxy/zaproxy:stable \
  zap-baseline.py -t http://127.0.0.1:5099 -J zap-report.json -I
```

基準：**0 FAIL / 8 WARN / 59 PASS**。要**逐項比對**，不能只看總數——
同樣的數字可能藏著一進一出。出現任何新的 finding 都視為發版阻斷。

目前僅存的一項 Medium 是 CSP 的 `unsafe-inline`（script 與 style），
因為整個前端都內嵌在 Python 字串裡。要移除必須重構模板，
那不是發版當下該做的變更。

---

## 第四層——瀏覽器人工清單

自動化測試操作的是 Flask 路由，不是 DOM。以下每一項都需要人與瀏覽器。
請在至少有一個代理程式的 Manager 上、**用兩種語言各跑一次**，
因為翻譯層是在畫面繪製後才改寫文字節點的。

**登入**——顯示 token 過期時間提示；Session 倒數計時器出現並遞減；
過期後回到登入頁而不是丟出堆疊追蹤；帳密錯誤時乾淨地失敗。

**代理程式**——列表載入；依狀態、作業系統、群組篩選；搜尋；
選取單一／多個／全部；重新啟動；刪除（兩道確認都要）；
升級到最新版、升級到指定版本、以上傳的 WPK 升級；升級結果回報；
代理程式詳細資訊；代理程式金鑰；執行期設定；Queue DB 大小與清除；主動回應指令。

**群組**——建立；刪除；加入與移除代理程式；全部移除；跨群組搬移；
獨占指派；設定的檢視、編輯、下載與匯入；檔案列表。

**節點**——列表與狀態；Daemon 統計；重新啟動服務；重新連線；
重新載入規則集；設定的檢視、編輯、驗證與下載；郵件告警設定；
升級檔案的列出、上傳與刪除；同步狀態與明細；記錄。

**規則**——五種模式全部：階層、所有規則、解碼器、CDB 清單、記錄測試。
以內容搜尋、以檔名搜尋（含只給一部份名稱、以及檔名加副檔名）。
檢視檔案；刪除規則；建立、編輯與刪除 CDB 清單。

**規則套件**——列表；詳細資訊要顯示檔案、指令碼、排程工作、代理程式群組，
以及任何無法完成 CDB 清單宣告的節點；
安裝；製造規則 ID 衝突後強制安裝；移除並確認被覆蓋的原檔有還原；
移除「安裝後檔案曾被修改」的套件，這種情況必須被拒絕。

**資產清單**——跨代理程式的套件查詢。

**統計**——摘要與報表。

**API 使用者**——列出；建立；刪除；變更角色。

**記錄**——檢視、篩選、下載。

**兩種語言**——每個分頁都切到 zh-TW。要注意 JavaScript 動態產生的內容
是否殘留英文，以及某個本該保持英文的字是否被過度翻譯。
`value` 與 `<option value>` 永遠不可被更動。

**離線**——在連不到 cdnjs 的環境下，設定編輯器會失去語法高亮，
但仍必須能載入、編輯與儲存。

---

## 第五層——部署

1. 部署後，**從目標路徑本身**把版本號讀回來確認。
   `tar` 沒帶 `-C` 就會解到別的地方，而讀錯副本的版本檢查會連續兩次回報成功。
2. 重新啟動服務並確認有起來。
3. 叢集環境：同步規則到 worker、重載兩個節點，並**在 worker 上再驗一次**。
   叢集會同步 `etc/rules` 與 `etc/lists`，但**不同步 `ossec.conf`**，
   而且不會重載 worker 的 analysisd。
4. `wazuh-analysisd -t` 必須乾淨，不能有新的警告。
5. 確認「應該要觸發的規則」仍然會觸發——清單改名或檔案搬移是**靜默失敗**的，
   載入時不報錯，重載時也不警告。

---

## 尚未涵蓋的部分

直接講清楚，因為一份暗示自己涵蓋範圍比實際更大的測試計畫，
比一份簡短的計畫更糟。

- **沒有 DOM 層級的自動化。** 前端只檢查語法是否可解析、翻譯是否一致。
  第四層的每一項互動都是人工的。截圖工具會用 mock 過的 API 繪製真實頁面，
  那裡是最自然的擴充起點。
- **破壞性路由的成功路徑不在 CI 中執行**，原因見第一層。
- **沒有負載或並行測試。** 上千台代理程式時的行為、
  或兩位操作者同時編輯同一份設定的行為，都未經量測。
- **沒有跨版本的升級測試。** 覆蓋安裝舊版是人工驗證的。
- **套件的偵測規則沒有在 CI 中以樣本記錄測試。** 它們是在 Manager 上
  用 `wazuh-logtest` 逐情境人工驗證的。同一父規則的子規則評估順序不可預期，
  所以單獨測試會過的規則，放進實際環境仍可能被別條規則遮蔽。
