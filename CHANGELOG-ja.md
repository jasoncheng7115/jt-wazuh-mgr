# 変更履歴

**jt-wazuh-mgr** の主な変更をここに記録しています。

[English](CHANGELOG.md) | [繁體中文](CHANGELOG-zh-TW.md) | [日本語](CHANGELOG-ja.md)

## v1.9.0 (2026-09-16)

- **新しいパック jt-alert-digest。** Wazuh マネージャーのメール経路は、
  たいていすでに存在し、すでに動いています。役に立たなくしているのは書式です。
  `wazuh-maild` は `full_log` をそのまま送り、続けて復号済みフィールドを
  もう一度すべて出力し、さらに 1 時間分のアラートを 1 通にまとめます。
  実機で測ったところ、1 通が 17 件の通知と数千行になり、
  その中で唯一の本物の検知は、ビルドホスト由来の 14 件の下に埋もれていました。
  このパックは同じルールと同じエージェントのアラートをまとめ、
  何が起きたかを示す少数のフィールドだけを出力し、実行ごとに 1 通送ります。
  同じ 17 件が約 40 行になります。

- 独自の設定はありません。`smtp_server`、`email_from`、`email_to`、
  `email_alert_level` はマネージャーの `ossec.conf` から読みます。
  そこは `wazuh-maild` のためにすでに設定されている場所なので、
  両者が食い違うことがありません。

- 本文は HTML で、プレーンテキストの代替も添えます。これは装飾ではありません。
  最初の版は空白で桁を揃えていましたが、どのメールクライアントも長い行を
  折り返し直し、揃えは崩れ、置き換える前と同じくらい読みにくくなりました。
  件名を ASCII だけに保っているのも同じ種類の理由です。非 ASCII が 1 文字でも
  入ると件名は MIME エンコード語になり、そのまま表示するクライアントがあります。

- ルール 131100 があるのは、通知の仕組みは自分の故障をメールでは知らせられない
  からです。失敗はマネージャーのイベントキューに書き込み、
  人がすでに見ているコンソールに出します。

- **jt-zimbra 2.6 は、v1.7.2 から自社マネージャーで動いていながら
  パックに戻されていなかった修正を取り込みました。** ルール 100807 は
  `jsp` から `elf` までのすべての拡張子を 1 本で扱い、ファイルモードを見て
  いませんでした。開発ホストで 15 日間に level 12 のアラートを 6,133 件生成し、
  そのうち 5,538 件が `.py`、多くは実行ビットのないソースコードで、
  そもそも実行できないものでした。現在は「その拡張子に実行ビットが要るか」で
  分割しています。100807 はモードに関係なくコンテナが読み込む
  Web アプリケーション成果物を、100809 はスクリプトとバイナリを担当し、
  実行ビットを要求します。検証済み: 0755 の `.py` は 100809 に一致し、
  同じファイルが 0644 なら何も出ません。

- **ビルドとテストのツールチェーンが使う一時ディレクトリを除外しました。**
  jt-portable-detect のルール 906234 と jt-zimbra のルール 100810 です。
  共用のビルドホストで測ったところ、1 日でその 1 台だけが level 12 を
  399 件生成し、環境全体の level 12 以上の 91% を占め、
  同じ日の本物の検知 36 件 — リバースプロキシに露出した設定ファイルを
  探すスキャナー — をその下に埋めていました。

- このルールにできないことも明記します。そのホストには該当ディレクトリが
  116 個あり、131,953 個のファイルを抱え、名前は各 CI スクリプトを書いた人が
  決めています。接頭辞の一覧では追いつきません。このルールが覆うのは
  サイトをまたいで安定しているツールチェーンの慣習までで、
  サイト固有のパスは予約された 906225-906229 の範囲に置いてください。
  ビルドホストではルールより手前で対処するほうが適切です。
  除外されたパスはアラートを生みませんが、ファイル整合性の項目は消費し、
  そのデータベースには全体の上限があります。

- **21 番目のリリース前チェック。** ビルド一時ディレクトリのパターンは
  2 つのパックにそれぞれ存在します。パックは独立して導入できるため、
  どちらも相手のルールがある前提には立てないからです。
  重複そのものは許容しますが、重複が黙って食い違うのは許容しません。
  preflight は両者が異なるとリリースを止めます。

## v1.8.0 (2026-09-14)

- **日本語に全面対応しました。** 画面、README 2 冊分にあたる文書、セキュリティ
  ポリシー、テスト計画、変更履歴の全体、プロジェクトのページ、そして
  トラブルシューティングのページが、English と 繁體中文 に加えて日本語でも
  利用できるようになりました。インストーラーはマシンの言語でトラブルシューティング
  ページを選ぶため、日本語環境からは日本語のページへ案内されます。

- 画面は 1 言語あたり 629 の文字列と、値を埋め込む 111 のパターンを翻訳します。
  エンジンはほとんど変更が要りませんでした —— `I18N[lang]` はもともと言語を
  キーにしていたためです —— 変わったのは切り替えボタンで、
  EN → 中文 → 日本語 と循環し、次に切り替わる言語を表示します。

- **翻訳パターンのうち 7 つは、何にも一致し得ないものでした。** 翻訳のために
  ブロックを 1 行ずつ読んだところ、正規表現リテラルが 7 つ、バックスラッシュを
  二重に書かれていることが分かりました。JavaScript の正規表現における `\\d` は
  数字ではなく「バックスラッシュに続く文字 d」に一致します。
  それらはルールパックとルールセット再読み込みのあたりで、書かれた時点から
  死んでいました。これを捕まえるものは何もありません —— ファイルは解析でき、
  項目は読み込まれ、文字列はただ英語のまま残ります。現在はテストが捕まえます。

- **新しいテスト 2 件と新しいリリース前チェック 2 件。4 つとも初回の実行で
  失敗しました。** 各言語は同じキーの集合を翻訳していなければなりません。
  さもないと、欠けたキーが翻訳済みのページの中に英語として現れます。
  文書は自身のすべての翻訳へリンクしていなければなりません ——
  誰かが拡張し続ける一覧ではなく、実際に存在するものを見て判断します。

- **7 つの死んだパターン以外に、新しいチェックが見つけたもの。**
  改名から 2 週間経ったあとも、旧製品名が `SECURITY.md` と両方の変更履歴の
  冒頭に残っていました。名称チェックが 5 つのファイルを名指ししていただけで、
  公開物全体を見ていなかったためです。現在は全体を見ます。
  `SECURITY.md` はサポート対象を 1.4.x のままにしていました。
  プロジェクトのページはテスト 163 件、ブラウザーチェック 62 件、
  シナリオ 17 本、機械的チェック 16 項目と書いており、4 つの数字すべてが
  3 リリース以上古いものでした。テスト計画にあるそのチェックの番号付き一覧は、
  11 項目で止まっていました。

## v1.7.5 (2026-09-14)

- **インストールとアップグレードのための検索可能なトラブルシューティングページと、
  そこへ案内するインストーラー。** スクリプトは `set -e` で動作するため、
  最初に失敗したコマンドで処理が終了します —— そのコマンド自身のエラーが
  すでに画面外へ流れたあとであることも珍しくありません。
  利用者に見えるのはプロンプトだけで、説明は何もありませんでした。

- インストーラーはその失敗を捕捉し、行番号と終了コード、そして該当するページの
  アドレスを表示するようになりました。これまで検出できなかったケースにも対応します。
  `systemctl start` はユニットが起動された時点で成功を返すため、起動直後に落ちた
  サービスは「インストール完了」と報告されていました。現在は待機して確認し、
  直近 15 行のログを表示します。

- **リンクはマシンの言語で選ばれます。** 中国語環境のシステムには中国語のページを、
  それ以外には英語のページを表示します。`curl ... | sudo bash` は通常、環境変数を
  リセットしてしまうため、ロケールはまずシェル変数から、次に `/etc/locale.conf`
  または `/etc/default/locale` から読み取ります —— 後者こそが sudo を越えて残るものです。

- ページは、インストーラーが実際に何をしていて、実際にどこで壊れるのかに答えます。
  Debian 12 と Ubuntu 23.04 以降の `externally-managed-environment`、`pip` が無い、
  または PyPI に到達できない、隔離されたマネージャーでのダウンロード遮断、
  サービスが起動しない、何も変わっていないように見えるアップグレード、
  そしてオフラインでのインストールとアップグレードの完全な手順です。
  目次と、見出しだけでなく回答全体を対象とする検索ボックス —— 利用者は与えられた
  エラー文をそのまま貼り付けるからです —— そして他言語版へのリンクを備えています。

- 2 つの回答は不具合ではなく仕様を説明しています。知っていること自体が解決だからです。
  `config.yaml` は決して上書きされないこと、そしてサービスのユニットは実行のたびに
  書き換えられるため、カスタマイズした設定はアップグレードが触れない systemd の
  drop-in に置くべきであることです。

## v1.7.4 (2026-09-09)

- **パック自身のファイル監視が、その周囲のファイル整合性監視を壊していました。
  そしてそれは、パックが出していたアラートよりも重大でした。**
  あるビルドホストで計測したところ、`/tmp` には 104,019 個のファイルがあり、
  Wazuh の FIM データベース全体の既定上限 100,000 を超えていました。
  エージェントのログは "Reached maximum files limit monitored" と
  "Real-time inotify kernel queue is full. Some events may be lost." で埋まります。
  結果はノイズではありません —— `/etc`、`/bin`、`/usr/bin`、`/root/.ssh` が
  確実に監視されなくなり、その 2 行の警告以外に知らせるものは何もありません。

  エージェント設定は、一時ディレクトリ配下の再帰を深さ 2 に制限するようになりました。
  104,019 個は 2,021 個になり、パックが探している形 —— インストーラーが
  `/tmp/<何か>/setup` に展開する —— は依然として深さ 2 にあります。
  そのホストで誤検知を生んでいたものは、すべて深さ 3 以上でした。

- **syscheck がハッシュを計算できなかったファイルは、実行ファイルとして扱われなくなりました。**
  同じホストは 5 日間でレベル 10 のアラートを 3,755 件出し（1 日の最大は 2,791 件）、
  そのうち 3,496 件 —— 93 パーセント —— が同じ形をしていました。
  権限 `rwxrwxrwx`、サイズは 0 ではない、そして SHA-256 は「内容がまったくない」もの。
  これはシンボリックリンクです。シンボリックリンクの権限は常に 0777 なので、
  権限だけを見るルールにはすべて実行ファイルに見えます。pytest だけで 1,957 件を
  占めており、実行のたびに `pytest-current` リンクを書き換えていたためです。

  これは `/dev/shm` の教訓を別の方向から見たものです。前回は権限ビットが本物で
  意味がなかったのに対し、今回はその権限ビットがファイルではなくリンクのものでした。
  正直な判別材料はハッシュです —— プログラムには内容があり、Wazuh はそれを
  ハッシュ化できたはずだからです。

- 併せて除外したもの: すべてのリポジトリが権限 0755 で同梱する git のフックのサンプル
  （194 件を占めていました）と、資産系のファイル種別（カラープロファイル、フォント、
  Web 資産）を既存の非実行ファイル拡張子一覧へ追加しました。

- トレードオフは `/dev/shm` と同様にルール内へ明記しています。監視対象外の場所に
  置かれたペイロードを指す、一時ディレクトリ内のシンボリックリンクは、この除外に
  該当してしまいます。実行そのものはルール 906211 が捉えます。こちらは
  ファイル権限では偽装できない証拠を auditd が裏づけています。

## v1.7.3 (2026-09-08)

- **Linux の実行系ルールに初めてデータが入り、v1.7.1 で書いた主張の 1 つが
  誤りだと分かりました。** どちらも同じ作業から得られたものです ——
  auditd が動いているホストにパックの監査ルールを入れ、実際のイベントが
  通っていく様子を見たのです。

- **実際に足りなかったのはファイル 1 つでした。** パックのエージェントグループに
  属する 2 台は実行イベントを出せません —— 1 台は 2 月から切断されたままで、
  もう 1 台には監査デーモンがありません —— 一方、auditd が動いている 2 台は
  そのグループに属していませんでした。属する必要もありませんでした。
  既存のグループがすでに `/var/log/audit/audit.log` を送っており、
  マネージャー側のルールはグループ所属ではなく監査キーを見ているからです。
  パックのルールファイルを `/etc/audit/rules.d/` へ入れることが対処のすべてで、
  次に `/tmp` から実行が起きた時点でルール 906211 がレベル 12 で発火しました。

- **v1.7.1 の訂正。** そのリリースでは「`audit.exe` と `audit.execve.*` は同じ
  イベントに現れることはない」と記録し、それを前提にルールを書き直しました。
  実イベントがそれを覆します。1 件のアラートが、SYSCALL レコードから `exe`、
  `key`、`uid` を、EXECVE レコードから `execve.a0` と `a1` を、
  PATH レコードから `file.name`、`file.inode`、`file.mode` を同時に持っていました。
  Wazuh はそれらを組み立てます。誤りは方法にありました ——
  `wazuh-logtest` は 1 行を再生するだけなので、常に 1 つのレコードの
  フィールドしか見せられず、どのフィールドが共起するかについて結論を
  導くことはできません。ルール自体は現在も機能しており変更していません。
  誤っていたのは、そこに書かれた理由づけだけです。

- **実行系のアラートが、解釈系ではなくファイルの名前を示すようになりました。**
  `/tmp -p x` の監視はパスに対して発火しますが、`audit.exe` が保持しているのは
  実行されたイメージです。shebang 付きのスクリプトでは `/usr/bin/dash` となるため、
  アラートは「シェルが一時ファイルシステムから実行された」と告げるだけで、
  読む人に何も伝えていませんでした。監視に一致したファイルは `audit.file.name` です。
  現在はファイルを先に、両方をアラートへ含めています ——
  実際のエージェントで検証し、同じ検証用ファイルの表示が
  `- /usr/bin/dash uid: 0` から
  `- /tmp/jt-portable-probe2.sh via /usr/bin/dash uid: 0` へ変わりました。

## v1.7.2 (2026-09-08)

- **誤検知の発生源を 3 つ、推測ではなく 47 日分の本番データで計測して特定しました。**
  パックが実際に何を出していたかを見直すことは、v1.7.1 で追加した網羅よりも
  価値がありました。同じ見直しによって、その追加分はこの環境ではほとんど
  発火しないことも分かったからです。

- **Windows: WinGet と DISM がレベル 10 でアラートを出していました。**
  ユーザーの Temp ディレクトリからの実行に関する 37 件のアラートのうち、
  4 件は WinGet がインストール中のパッケージを一時展開したもの、
  3 件は Windows のサービスコンポーネントである `DismHost.exe` でした。
  マイクロソフト自身のツールに対して誤るレベル 10 のルールは、
  「レベル 10 には意味がない」と人に教えてしまいます ——
  このパックがファイル系ルールについてすでに記録していた教訓が、
  実行系の側で再び現れたかたちです。2 つの除外はいずれも、それらのツールが
  生成するパスの形に一致させており、ファイル名では判定しません。
  サービス用の GUID ディレクトリの外にある `DismHost.exe` という名前のバイナリは、
  今も変わらずアラートになります。

- **インベントリがパッケージ管理のキャッシュを未署名の実行ファイルとして報告していました。**
  そのルールの件数の 4 分の 1 —— 221 件中 53 件 —— が `uv` によるものでした。
  同梱の CPython インタープリターと展開された wheel のキャッシュで、
  未署名なのは Python の配布物がそうだからであって、誰かがツールを
  持ち込んだからではありません。パックはまさにこれらのパスをすでに除外していましたが、
  それはファイル整合性のルールの下だけで、インベントリのルールはその除外に
  追加されていませんでした。v1.7.1 の Linux 実行系ルールと同じ形の誤りです ——
  ファイル系には 4 つの除外があるのに実行系には 1 つもなかった、
  つまり除外は存在するのに、その誤検知を生むすべての親ルールに紐づいていない、というものです。

- 稼働中の 4.14.7 マネージャーで検証しました。インベントリの除外は `wazuh-logtest`
  で確認し、同じディレクトリツリーにある本物の可搬ツールである Ventoy は
  引き続き報告されること、ハッシュと署名の不一致は依然としてレベル 12 で
  報告されることも併せて確認しました。Windows の比較パターンは、
  本番アラートから取り出した実際のパスに対して確認しています。

- 記録しておく価値があること: 後から読み込まれるファイルで `overwrite="yes"` により
  親ルールが置き換えられても、その子ルールは動作し続けます。この環境では
  インベントリのルールをその方法で格下げしており、新しい除外は置き換え後の
  ルールへ正しく紐づきました。推測ではなく、テストで確認しています。

## v1.7.1 (2026-09-08)

- **可搬実行ファイルの検知が、「ファイルがどこにあるか」だけでなく
  「プログラムが実行されたか」を問うようになりました。** パックへの 6 つの変更は、
  いずれも網羅のために足したものではなく、計測して誤っていた点を直したものです。

- **Windows: 可搬プログラムが実行される 5 つの経路が見えていませんでした。**
  ユーザーディレクトリ向けのルールは `Public` をパスの 3 番目のセグメントに
  挙げていました。それが表すのは `C:\Users\<ユーザー>\Public\` であり、
  Windows にそのようなディレクトリはありません。本来の共有フォルダー
  `C:\Users\Public\` は 1 つ上のセグメントにあるため、まったく捕捉されておらず、
  この分岐は書かれた時点から何にも一致していませんでした。併せて追加したのは、
  ユーザープロファイルの直下、システムの Temp ディレクトリ、ProgramData、
  そしてネットワーク共有からの直接実行です。最後の 1 つが最も重要でした ——
  この区画のルールはすべて `^[A-Z]:` で始まっていたため、UNC パスに一致できる
  ルールが存在せず、共有からツールを実行することは可搬プログラムが持ち込まれる
  ごく普通の経路の 1 つだからです。

- **Windows: どのように起動されたかを読む新しいルール。** Downloads に置かれた
  ファイルは、それが使われたかどうかを何も語りません。親プロセスが
  `explorer.exe` であるプロセスは、人がダブルクリックしたものです。
  それこそがこのパックの存在理由である問いですが、これまで誰もそれを
  問うていませんでした。

- **Linux: 実行系のルールには除外が 1 つもありませんでした**（ファイル系には 4 つ）。
  Ansible は実行するモジュールをすべて一時ディレクトリへ配置してそこで実行するため、
  playbook を 1 回流すとタスクごとにレベル 10 のアラートが 1 件出ます。
  Salt、Chef、Puppet、cloud-init、RPM のインストール後スクリプトも同じ形です。
  2 つの除外でこれらを覆いました。攻撃者が自由に選べるプロセス名ではなく、
  それらのツールが生成する一時ディレクトリのパスに一致させています。
  `/dev/shm` は意図的に対象外です。そこで作業を展開する構成管理ツールはありません。

- **Linux: 解釈系を通して実行されるスクリプトが見えていませんでした。**
  `-p x` の監査ウォッチはファイルへの実行アクセスで発火するため、
  `bash /tmp/tool.sh` では決して発火しません —— 実行されるのは `/bin/bash` であり、
  スクリプトは読み取られるだけだからです。可搬ツールのかなりの割合はスクリプトです。
  新しいルールは、解釈系のパスではなくその引数を読みます。

- **決して発火し得なかった相関ルールを修正しました。** 繰り返し実行を見るルールが
  `same_source_ip` を持っていましたが、これは auditd イベントに存在しない
  フィールドを要求します。auditd が記録するのはローカルのプロセス実行であり、
  ネットワークの相手側は存在しません。Wazuh 自身の 2 つの auditd ルールファイルも
  この条件を 1 度も使っておらず、このパックの Windows 側の対応ルールも使っていません。

- 稼働中の 4.14.7 マネージャーで検証しました。`wazuh-analysisd -t` はルールセットを
  問題なく読み込みます。`wazuh-logtest` は Linux のルールをシナリオごとに確認し、
  *一致してはならない* ケースも含めています。Windows の比較パターンは、
  本番から取り出した実際のフィールド値 —— 区切りごとにバックスラッシュが
  2 つ入るもの —— に対して確認しました。

- この作業中に記録したこと（書き直しを 1 度要したため）: `audit.exe` と
  `audit.execve.*` は同じイベントに現れません。auditd は 1 回の実行につき
  SYSCALL レコードと EXECVE レコードを出し、Wazuh はそれらを 2 つの独立した
  イベントとして解釈し、両者を結び付けるものは何もありません ——
  そのため、両方からフィールドを指定したルールは問題なく読み込まれ、
  そして決して発火しません。

  *（訂正: この記述は v1.7.3 で誤りであることが判明しました。実イベントでは
  両方のフィールドが同じアラートに現れます。当時の結論は、1 行だけを
  `wazuh-logtest` に与えたことによる思い違いでした。）*

## v1.7.0 (2026-09-05)

- **本ツールは Wazuh 4.x と 5.x の両方で動作し、どちらかをサーバーに尋ねて判断します。**
  サーバーのバージョンは接続時に一度だけ読み取ります —— `/cluster/nodes` を使い、
  だめなら `/cluster/local/info`、次に `/manager/info` へ（最後のものは 4.x にしか
  存在しないため、最初に試すことはできません）—— そしてそれを、各ルートが参照する
  機能テーブルへ変換します。

  これは「4 か 5 か」の切り替えではありません。各機能が「どのバージョンで追加され、
  どのバージョンで削除されたか」を持つため、将来のリリースへの対応は、
  あちこちに散らばった判定を探し回ることではなく、境界を 1 つ足すことになります。

- **なぜ必要だったか。** Wazuh 5.0-beta5 は、本ツールが呼び出している 49 の API
  エンドポイントのうち 21 を削除します。公開仕様と照合したところ、エンドポイントは
  150 から 73 になり、`/rules`、`/decoders`、`/lists`、`/logtest`、
  すべての `/manager/*`、`/syscollector/*`、`/active-response` が消えています。
  ソースツリーでは `etc/rules` が存在しなくなり、`analysisd` は XML ではなく YAML の
  ルールセットを持つエンジンに置き換えられ、CDB リストはキーバリューデータベースになります。

- **5.x サーバーではどう見えるか。** ルール、ルールパック、インベントリの各タブは
  非表示になります。その背後にあるものがすべて削除されたからです。エージェント、
  グループ、ノード、ユーザー、統計、ログの各タブはそのまま残ります。機能が失われた
  ルートは、存在しなくなったエンドポイントからの 404 をそのまま返すのではなく、
  サーバーのバージョンと何が欠けているかを示して 501 を返します。404 をそのまま
  返すと、サーバーのバージョン差ではなく本ツールの不具合のように読めてしまうからです。

- **バージョンが読めないときは、すべてを有効にします。** 文字列を解析できなかったと
  いう理由で機能を黙って無効化するツールは、試してみて実際のエラーを報告するツール
  よりも原因を追いにくくなります。このリリース以前に作られたセッションが動き続けるのも
  同じ理由です。

- 稼働中の 4.14.7 マネージャーで検証しました。`4.14.7` を検出し、11 の機能が利用可能、
  1 つ —— 5.x 専用のエンドポイント —— が正しく利用不可と判定されました。
  5.x の挙動は、ブラウザーシナリオ側の 2 つ目のモックサーバーで確認しています。
  実際の 5.x サーバーがない状況では、それが唯一の検証手段です。

- テスト 178 件、19 シナリオにわたるブラウザーチェック 81 件。そのうち 12 件が
  バージョン層を対象としており、アプリケーション自身のルートマップから導出したものも
  含まれるため、後から追加されたルートがガードを黙って素通りすることはありません。

## v1.6.21 (2026-09-04)

- **/dev/shm の変更を、稼働中のエンジン上で端から端まで検証しました。**
  マネージャーで一時的に `/dev/shm` を監視し、ファイルを 2 つ作って決着をつけました。
  `jt_probe_kdevtmpfsi` はルール 906203 をレベル 6 で発火させ、
  `jt_probe_mailbox_0_shm` は何も発火させず、意図どおり 906224 に抑制されました。
  設定はその後もとに戻しています。

  これが重要だったのは、このルールを他の方法では確認できないからです。
  `wazuh-logtest` はログ行をデコーダーへ通して再生しますが、ファイル整合性の
  イベントはマネージャー内部で生成され、デコーダーを一度も通りません。
  そのため一般的な JSON として解釈され、どのルールにも到達しません。
  実際のイベントが再発するのを待っても、何かを証明することにはなりません —— 下記参照。

- **v1.6.20 の記述の訂正。** そこでは 53 件の誤検知が「1 週間にわたって」発生したと
  書いており、これは少しずつ出続けたように読めます。実際には 1 台のホストで
  4 時間のうちに一気に発生し、その前後 7 日間には 1 件もありませんでした。
  修正内容は変わらず、件数を根拠にしない分むしろ理由づけは強くなります ——
  53 件はすべて誤りであり、ファイル整合性監視は構造上、ELF と共有メモリーセグメントを
  区別できないからです。頻度は関係ありません。そして「一気に出る」形は、
  アプリケーションが再起動したときの姿そのものなので、また繰り返されます。

- **README からリリースノートを削除しました。** 英語版には「What's New」という節があり、
  すぐ上のバッジが 1.6.19 を示しているのに、そこには 1.6.1 が並んでいました ——
  リリースを記録する場所が 2 つあるということは、忘れる場所が 2 つあるということです。
  そうした節が復活したらリリース前チェックが失敗するようにしました。
  記録の場所は変更履歴ただ 1 つで、README はそこへリンクします。

## v1.6.20 (2026-09-04)

- **可搬実行ファイル検知の Linux 側に、発火するたびに必ず誤っているレベル 12 の
  ルールがありました。** ルール 906203 は、`/dev/shm` 配下に実行ビットの付いた
  ファイルが現れると必ずアラートを出していました。実環境では 1 台のホストで
  4 時間のうちに 53 件を出し、その前後 7 日間は 1 件もありませんでした。
  53 件すべてが POSIX 共有メモリーオブジェクト —— あるメールアプリケーションが
  権限 0777 で作成するセグメント、ミューテックス、イベントオブジェクト —— であり、
  それは `shm_open()` にとって正常な動作です。一気に出る形が重要です。
  これはアプリケーションが再起動したとき、あるいはファイル整合性監視が
  そのディレクトリを初めて見たときのホストの姿であり、一度きりではなく繰り返されます。

  このルールは権限について正しく、その意味について誤っていました。
  ファイル整合性監視が報告するのはパス、権限、サイズ、ハッシュであり、
  ファイルのマジックバイトは決して見ないため、ELF バイナリと共有メモリーセグメントを
  区別できません。現在は実際に分かっていることを述べるレベル 6 になり、
  ルール 906224 が IPC の命名慣習を除外します。実在した 53 件のパスすべてと、
  ドロッパーが使いそうな `kdevtmpfsi`、`xmrig` ほか 5 つの名前に対して検証済みで、
  後者は引き続きアラートになります。

  `/dev/shm` からの実行は、ルール 906211 として引き続きレベル 12 です。
  auditd は、ファイルが現れたことではなくプログラムが実行されたことを証明するからです。

- **パックが、その Linux ルールの前提となるエージェント側の収集設定を同梱するように
  なりました。** これまで 1 つもありませんでした。コメントで参照されていた監査ルール
  ファイルは存在せず、そのためルール 906210 と 906211 は決して発火せず、
  ファイル系のルールも、誰も設定していない監視に依存していました。

  追加したもの: エージェントグループ `portable-detect`。`/tmp`、`/var/tmp`、
  `/dev/shm` をリアルタイムでファイル整合性監視し、ユーザーのダウンロード
  ディレクトリは定期スキャンで、そして監査ログを収集します。さらに auditd 用の
  `jt-portable.rules` を同梱し、マネージャー上に置いたままにせず、グループ
  ディレクトリに同梱してエージェントへ届くようにしました。`/home` の監視は
  コメントアウトして同梱し、その理由も明記しています —— 正確だが騒がしく、
  そのトレードオフは運用者が決めるべきものだからです。

- **ルール 906212**: `/tmp` から一度だけ実行されるプログラムはインストーラーかも
  しれませんが、10 分間に 6 回実行されるものは、何も住むべきでない場所に
  住み着いているということです。

- パックのエージェントグループが `agent.conf` 以外のファイルも持てるようになりました
  （`agent_group.files` に列挙します）。名前は検証され、既存のファイルは決して
  上書きされず、ロールバックされたインストールはそれらも一緒に取り消します。

- エージェント設定のリリース前チェックが、正しいファイルを拒否していました。
  `agent.conf` は断片であり、`os` 属性の異なる `<agent_config>` ブロックが
  複数並ぶのが通常の形です。現在は 1 つのものとして解析します。

- 稼働中の 4.14.7 クラスタで検証しました。`wazuh-analysisd -t` はクリーン、
  両ノードとも警告なしで再読み込みされ同じファイルを保持しており、
  各シナリオをマスターで `wazuh-logtest` により確認し、ワーカーでも再度確認しました ——
  パックに一致してはならない否定ケースも含めています。

## v1.6.19 (2026-09-01)

- **AGPL-3.0 へライセンスを変更しました。** 本プロジェクトは Apache-2.0 でしたが、
  これはクローズドな派生物を許容します。GPL-3 ならその配布を止められますが、
  本ツールはホストされる管理コンソールであり、GPL の義務は配布によって発生します ——
  改変版を他者向けのサービスとして動かすことは配布にあたりません。
  AGPL 第 13 条がそこを塞ぎ、画面には同条が求めるソースコードの提供を掲げました。

- **ブラウザーシナリオの自動化。** `tests/e2e/run.sh` は、モック化され状態を持つ
  Wazuh API に対し、実際のブラウザーで 17 のシナリオと 65 のチェックを実行します。
  ユニットテストは Flask のルートには届きますが、フロントエンドについては
  解析できることしか確認できません。人が行うすべて —— タブの切り替え、行のチェック、
  ダイアログの確認、表の読み取り —— は、何もテストされていないコードを動かしていました。

  この仕組みは、本番と同じ方法でアプリケーションを起動します。初期版は
  `app.run()` を直接呼んでいたためヘッダーの堅牢化が飛ばされ、あるチェックが
  すぐに「`Server` ヘッダーが werkzeug のバージョンを広告している」と報告しました。
  それは、出荷されるものとは別のプログラムを計測していたということであり、
  起動経路を共通化した理由です。

  これらを書く過程で、インターフェースについて 1 つ分かったことがあります。
  画面はタイマーで更新され、ダイアログへ入力してからボタンを押すまでの間に
  更新が挟まるとモーダルが再描画されます。少し前に取得したダイアログは、
  押された時点でボタンを失っていました。

- **リリース前チェックを 5 つ追加**しました。いずれも初回の実行で失敗しました:

  README の見出しにはバージョンが入っていますが、それを見ているものが何もなく ——
  9 リリース経ったあとも v1.6.0 のままで、その下のバッジは正しい値でした。
  ライセンスはどこでも同じ表記であること（バッジは AGPL、7 つのパックの
  マニフェストは Apache、という状態ではなく）。プロジェクト名は 3 通りではなく
  小文字の `jt-wazuh-mgr` 1 通りであること。アイコン一式が存在し参照されていること。
  画面がソースコードを提供していること —— AGPL のもとでは、これは親切心ではなく
  ライセンス条項です。

- **プロジェクトのアイコン。** これまでの標章は作者の会社ロゴでした。今回のものは
  プロジェクト自身のものです。エージェントの群れを抱えるマネージャーを、
  16 ピクセルでも成立するように描き、SVG、6 サイズの PNG、
  そして複数サイズを含む `.ico` として同梱しています。

- **プロジェクトのページが、このプロジェクトの内容を説明するようになりました。**
  ルールパックにはまったく触れられていませんでしたが、それは Wazuh が答えを
  持っていない部分です。追加したもの: 7 つのパックとそれぞれが何を検知するか、
  クラスタが同期するものと利用者に委ねるもの、そしてリリース前に何を検証し、
  何を検証していないか。

- 公開名は、画面上を含めてすべて `jt-wazuh-mgr` になりました。

## v1.6.18 (2026-09-01)

- **ルールセットの再読み込みが、analysisd の警告を表示するようになりました。**
  あるノードが読み込めない CDB リストがある状態で再読み込みすると、こう返ります:

  ```
  error: 0   message: "ok"
  data: ["List 'etc/lists/x' could not be loaded. Rule '199990' will be ignored."]
  ```

  成功した再読み込みが、「たった今ルールを 1 つ無効にした」という知らせを
  抱えているわけです。ノードごとの再読み込みは `failed_items` しか読んでいなかったため、
  きれいな成功として報告し、この警告を捨てていました。現在はその内容を返し、
  トーストではなくダイアログで表示します。読み込まれても決して一致し得ないルールは、
  手を止めるに値するからです。

  これは推測ではなく計測によるものです。本番クラスタで、宣言されていないリストを
  読む検証用ルールがワーカー側でまさにその警告を出し、何にも一致せず、
  宣言を追加した瞬間に一致し始めました。

- **クラスタ全体の再読み込みが、単なる成功文を警告として並べなくなりました。**
  これまではクリーンな再読み込みのたびに、すべてのノードの警告欄へ
  "Ruleset reload request sent successfully." と表示していました。
  それは、本物の警告が現れる欄を読み飛ばす習慣を人に教えてしまいます。

- v1.6.17 の記述では、宣言されていないリストは「読み込み時にエラーも出ず、
  再読み込み時にも警告が出ないまま無視される」と書いていました。前半は正しく、
  後半は誤りです。analysisd は警告を出します —— それ以外は成功している
  再読み込みの中で。ここと、テスト計画と、コード中のコメントで訂正しました。
  このリリースが存在する理由でもあります。情報は最初からそこにあり、
  本ツールがそれを捨てていたのです。

## v1.6.17 (2026-09-01)

- **CDB リストを同梱するパックが、ツールを実行しているノードだけでなく、
  クラスタの全ノードで宣言されるようになりました。** クラスタは `etc/rules`、
  `etc/decoders`、`etc/lists` を同期しますが、`ossec.conf` は `ar.conf` とともに
  `excluded_files` に入っています。そのため、ルールとリストは各ワーカーへ届く一方で、
  それらが依存する `<list>` の宣言はマスターに取り残されていました ——
  そしてリストが宣言されていないルールは、読み込まれはしても決して一致しません。
  analysisd はそのことを伝えますが、それ以外は**成功している**再読み込みの中の
  警告としてです。エラーではなく、何かを止めることもなく、見落としやすいものです。
  エージェントのイベントを処理するのはワーカーなので、マスター＋ワーカー構成の
  クラスタでは、その検知は単に動いていませんでした。

  7 つのパックのうち 4 つが CDB リストを同梱しており、影響を受けていました:
  `jt-ioc`、`jt-malware-hash`、`jt-portable-detect`、`jt-zimbra` です。

  宣言は Wazuh API（`PUT /cluster/{node_id}/configuration`）経由で送られるように
  なりました。これには事前設定が不要です。失敗した場合は、ノード設定エディターが
  すでに使っている任意のノード別設定と同じものを用いて SSH へフォールバックします。
  そして各書き込みは**読み戻して**確認します。200 が意味するのはアップロードが
  受理されたことであって、ファイルが送った内容を保持していることではないからです。

- **ワーカーへ到達できなかったときの挙動が、既定任せではなく明示的な判断に
  なりました。** インストールは中断してロールバックし、ノード名、アドレス、理由、
  そして何をすべきかを示します —— そのノードの SSH を設定するか、
  `<list>` の項目をそのノードの `ossec.conf` へ手作業で追加して再読み込みするか。
  このノードだけにインストールするなら、`local_only` を付けて要求を繰り返します。

  それを選んだ場合、不足しているノードがパックのインストール状態に記録され、
  パックの詳細を開くたびに毎回表示されます。インストール時に一度だけ表示された
  警告は、誰かが「なぜワーカーがアラートを出さないのか」と考え始めるころには
  もう消えています。

- パックの削除時には、他のノードからも宣言を取り下げ、到達できなかったノードは
  黙って宣言を残すのではなくログに記録します。

- テストを 13 件追加し、合計 159 件になりました。宣言テキストそのもの、
  対向ノードへの書き込み経路、書き込みを受理しても適用しないノード、
  重複した宣言が二度目には何も書かないこと、中断とロールバックの経路、
  そして `local_only` を対象としています。

## v1.6.16 (2026-09-01)

- **入力検証の抜けが 3 つ、足りていなかったテストを書いたことで見つかりました。**
  82 のルートのうち 45 には、どんなテストもありませんでした。それらを書いたところ、
  実際の不具合が 3 つ出てきました。いずれも状態を変更するルートです:

  `..` が有効なグループ名・ノード名として通っていました。両方の検証器の背後にある
  パターンは `.` を許可しているため、ドットだけで構成された名前が一致してしまい、
  そうした名前は API の URL とパスへ埋め込まれます。両方の検証器は現在、
  `.`、`..`、および `..` を含む名前を拒否します。

  グループ所属を変更する 2 つのルートは、渡されたエージェント ID を検証せずに
  そのまま転送していました。まさにこのための正しいヘルパーがすでに存在し、
  他の 6 つのルートでは使われていたにもかかわらずです。

  3 つのうち最も深刻なもの: Queue DB のクリアは `agent_ids` を直接反復していました。
  文字列は反復可能なので、`{"agent_ids": "001"}` は `['0', '0', '1']` になります ——
  それぞれが検証を通る 3 つの ID であり、このルートは与えられたすべての ID について
  queue DB ファイルを削除してエージェントを再起動します。エージェント `0` は
  マネージャー自身です。4 つのルートはすべて共有ヘルパーを使うようになり、
  空でないリストを必須とします。

- **2 つ目のテストモジュール `tests/test_ui_operations.py` を追加**しました。
  状態を変更するルートを対象としています。グループ、ノード、エージェント、パック、
  設定の各操作にわたる 20 件のテストで、安全でない名前、パストラバーサル、
  リストでない本文、XML 外部実体、そしてパスではなくキーワードを取る
  ファイルダウンロードのルートを扱います。

  破壊的なルートは拒否経路のみを実行します。それらの多くは検証を通過したあとに
  外部コマンドを実行したりファイルを削除したりしますし、これらのテストを実行する
  マシンはたいてい稼働中のマネージャーです。主張する価値があるのはガードの部分です。

  認証のテストは、手書きの 8 件の一覧ではなく、アプリ自身の `url_map` から
  ルート一覧を導出するようになりました。これにより、後から追加されたルートも
  誰かがテストを足すのを覚えていなくても対象になります。テストは 146 件です。

- **テスト計画を両言語で用意しました**（[TEST-PLAN.md](TEST-PLAN.md)）。
  5 つの層 —— 自動テスト、機械的チェック、セキュリティスキャン、ブラウザーの
  チェックリスト、配備 —— をすべて扱い、まだ網羅できていないことも明記しています:
  DOM レベルの自動化なし、負荷試験なし、バージョン間のアップグレード試験なし、
  検知ルールは CI ではなく手作業で検証。

- **IOC パックの IP リストを `jason_tools_threat_ip` に改名しました。**
  以前は `jason_tools_blacklist` でした。この語には、そのファイルの内容とは
  何の関係もない人種的な含意があり、脅威フィードで観測されたアドレスの一覧は
  その語を使わずとも十分に言い表せます。更新プログラムも名前を合わせ、
  30 か所のルール参照もすべて移行しました。

  CDB リストの改名は文字列置換では済みません。クラスタは `etc/lists` を同期しますが
  `ossec.conf` は同期しないからです。各ノードには自分の `<list>` 宣言が必要で、
  宣言されていないリストを指すルールは**静かに**無視されます ——
  読み込み時のエラーもなく、再読み込み時の警告もなく、ただ発火しないルールが残ります。
  両方の宣言は当面あえて残しています。新しい名前が十分に動いたと確認できてから、
  古いリストを削除します。

- **誤って落としていた 2 つの脅威フィードを復活させました。** v1.6.14 で更新
  プログラムを書き直した際、元のソースのうち 3 つを静かに失っており、リストは
  259,839 件から 220,957 件へ減っていました —— 15% の網羅の喪失を、何も報告しません
  でした。短いリストもやはり有効なリストだからです。Tor 出口ノードと
  matthewroberts の脅威リストを復旧しました。3 つ目の maltrail は上流で
  本当に消滅しています。リストは 247,383 件に戻りました。

  更新プログラムは、件数が半分以上減ったリストのインストールをすでに拒否します。
  その閾値は今回を捉えるには緩すぎましたし、そもそもこのチェックは前回の実行との
  比較しか行わないため、スクリプトを書き直したその変更で生じた喪失を、
  そのスクリプト自身が捉えることはできません。現在はフィードを 1 行ずつ、
  それぞれの寄与件数とともに列挙しているため、欠けているものは出力を数えなくても
  ファイルを読めば見えます。

- **Zimbra のルール 100990 は修理ではなく削除しました。** これはプレースホルダーが
  1 件だけ入った状態で同梱されていたハッシュリストに一致するもので、
  レベル 15 のルールでありながら一度も発火しておらず、発火し得ませんでした。
  調べたところ、さらに 2 つの不具合が出てきました。`sha256` という名前の
  フィールドを読んでいたこと（ファイル整合性イベントが持つのは
  `syscheck.sha256_after` です）、そして jt-malware-hash パックのルール 100141 が
  すでにまったく同じ仕事を、毎晩再構築される約 110 万件のハッシュ一覧に対して
  行っていることです。ファイル整合性イベントに対するハッシュ照合には、
  そちらのパックを導入してください。

- **承認済み可搬実行ファイルの許可リストが、本当に空の状態で同梱されるように
  なりました。** これまでは捏造された項目が 1 件入っており、リストが埋まっている
  ように見せながら、実際には何も承認していませんでした。許可リストの正しい既定値は
  空です。ファイルには代わりにキーの書式を記載しました —— Sysmon が報告するとおり、
  `SHA256=` の接頭辞を含む `hashes` フィールド全体です。これは自明ではなく、
  以前は偽の項目の形からしか伝わっていませんでした。

- プレースホルダーを含んだままの CDB リストがあると、リリース前チェックが失敗する
  ようになりました。一致し得ないリストを指すルールを、二度と公開できません。

## v1.6.15 (2026-09-01)

- **可搬実行ファイル検知: 実トラフィック 1 週間分で調整しました。** パック自身の
  ルールは問題ないことが分かりました —— 実際のホストで Downloads と Temp から
  バイナリを実行し、意図どおりルール 906100 と 906102 が発火することを端から端まで
  確認しています。1 週間沈黙していたのは、その環境で可搬実行ファイルを動かす人が
  いなかったからで、それは「壊れている」こととは別のことです。

  壊れていたもの、そして騒がしかったものは、どちらも別の場所にありました。
  ルール 906120、906121、906122 は発火し得ませんでした。1 つ目はどのエージェントも
  監視していない syscheck のパスを、残る 2 つは実際の構成が出力しない Sysmon の
  イベントを見ていたためです。そして Wazuh 自身の Sysmon ファイル作成ルールが
  7 日間で 4,245 件のアラートを出しており、その大半はノイズでした:

  | ルール | レベル | 7 日間 | 実際の正体 |
  |---|---|---|---|
  | 92205 | 9 | 3,122 | PowerShell が自身の実行ポリシーを確認していた |
  | 92217 | 6 | 799 | .NET のネイティブイメージ生成 |
  | 92200 | 6 | 278 | Windows のプールタグダンプ（毎回 2 回書かれる） |
  | 92213 | **15** | 33 | ブラウザーの更新プログラムが自身のダウンロードを展開 |
  | 92207 | 12 | 5 | Chrome のインストーラーが Public にショートカットを作成 |

  ルール 906160〜906166 がまさにそれらを抑制します。それぞれ「このプロセスは信頼
  できる」ではなく、特定のパスやファイル名に結び付けています。プロセス名は
  攻撃者が自由に選べるからです。実環境で検証したところ、計測期間中に 8 回の
  PowerShell スクリプト実行がありましたが、アラートは 1 件も出ませんでした ——
  以前はおよそ 2 分に 1 件の割合でした。

  毎回誤るレベル 15 のルールは、ルールが無いよりも悪いものです。
  「レベル 15 には意味がない」と人に教えてしまうからです。

- 906120 は書き直しではなく削除しました。Wazuh のルール 92200〜92217 がすでに
  ユーザーディレクトリへ実行ファイルが現れることを検知しており、同じグループの
  下にこちらが足したものは兄弟ルールとなって、どれか 1 つが一致した時点で
  到達されなくなります —— 実際のホストでファイルを作成し、ルール 92203 が
  それを取るのを見て確認しました。

## v1.6.14 (2026-09-01)

- **パックが、実際に動作するために必要なものを導入するようになりました。**
  これまでのパックは、ルール・デコーダー・リストをマネージャーへ置いてそこで
  終わりでした。それは、いくつかのパックが実際には何もしていなかったことを
  静かに意味します。jt-ioc は誰も埋めていない CDB リストに一致する 30 のルールを
  同梱していました。jt-fail2ban のルールは、エージェントが読まないログを
  見ることはできません。パックは今後、次の 2 つも持てます:

  **更新プログラムとそのスケジュール。** jt-ioc と jt-malware-hash はそれぞれ
  書き直した更新プログラムを同梱し、`etc/jt-packs/bin/` へインストールして
  `/etc/cron.d` 経由でスケジュールします。どちらも標準ライブラリのみを使うため、
  マネージャーに pip パッケージは不要です。また、どちらも全体で絶対パスを使います。
  置き換え対象だったジョブは相対パスを使っており、cron は `cd` なしで実行するため、
  10 か月半にわたって静かに失敗し続け、それが供給するリストは凍結したままでした。
  どちらも、項目の半分以上を失ったリストのインストールを拒否します。
  フィードの書式変更は、脅威状況が改善したのとまったく同じように見えるからです。

  **エージェントグループ。** jt-fail2ban と jt-zimbra は、そのルールが依存する
  収集設定を同梱します。同名の既存グループが上書きされることは決してありません ——
  すでにエージェントが割り当てられているかもしれませんし、パックの知らない設定を
  持っているかもしれないからです。

  root 権限でスケジュール実行される実行ファイルの導入は特権的な行為なので、
  マニフェストに明記し、インストール前の詳細ダイアログで表示し、結果にも報告し、
  アンインストール時には削除します。

- 機能していない maltrail フィードは、404 を永久にログへ出し続けさせるのではなく
  削除しました。また、予約済みおよびよく知られた公開アドレスは、フィードが何と
  言おうと除外するようになりました。

## v1.6.13 (2026-08-31)

- **fail2ban のログ収集を Zimbra グループから切り離しました。** パック自体は
  もともと独立していましたが、それを支える localfile の設定が Zimbra の
  エージェントグループへ追加されていたため、「fail2ban を監視している」が
  静かに「メールサーバー上の fail2ban を監視している」という意味になっていました。
  fail2ban は Web サーバー、踏み台、その他インターネットに面したあらゆるものの上で
  動きます。収集設定は独立したグループへ移し、そのホストが他に何をしていようと、
  fail2ban を動かしているエージェントに割り当てられるようにしました。

## v1.6.12 (2026-08-31)

- **新しいパック: fail2ban。** Wazuh は fail2ban 向けのデコーダーもルールも
  まったく同梱していません。これは推測ではなく確認した事実です。実際のログ行を
  `wazuh-logtest` に与えると、すべて "No decoder matched" が返ってきました。
  2 台のメールサーバーが何か月も攻撃者を BAN し続けていたのに、
  それを記録するものは何もありませんでした。

  BAN と、それを終わらせる解除の両方を報告します。あとから事案を読むときに
  意味を持つのは、その対でだからです。RFC 1918 アドレスの BAN は、外部アドレスの
  BAN より高く評価します。外部のアドレスが認証に失敗するのはインターネットが
  インターネットであるだけですが、内部のホストがそれをしているなら、
  古いパスワードのまま放置されたクライアントか、認証情報の一覧を試している
  マシンかのどちらかだからです。相関ルールは BAN を重ねる送信元を格上げし、
  内部ホストがそうしている場合は侵害の可能性として扱います。

  アドレスは `srcip` として解釈されるため、これらのイベントはルールセットの
  他の部分と相関し、アクティブレスポンスのコマンドからも利用できます。

- リリース前のプライバシーチェックが CIDR 表記を認識するようになりました。
  `/nn` が付いたアドレスはホストではなく範囲の定義であり、RFC 1918 の送信元を
  分類するルールは、その仕事をするためにそれらの範囲を書く必要があります。

## v1.6.11 (2026-08-31)

- **すべてのスクリーンショットを、モック API に対して撮り直しました。**
  公開されていた画像は実際のコンソールから撮られたもので、そのネットワークの
  完全な棚卸しを含んでいました。41 台のエージェント名、そのアドレス、
  オペレーティングシステム、エージェントのバージョン、クラスタノード名、
  アドレスバーに映るマネージャーのホスト名、そして運用者のログイン名です。
  現在はヘッドレスブラウザーで、完全にモック化した Wazuh API に対して生成しており、
  データは作り物です —— そして各画面が何のためのものかを示すように選んでおり、
  その点では実際のコンソールより優れています。

  3 か所の漏えいは、コードではなく描画された画像を見て初めて明らかになりました。
  ローカルノードにはホスト自身の名前が表示されること、ossec.conf ビューアーは
  ディスク上の実ファイルを読むこと、そしてログビューアーはアプリケーションが
  最後に書いた内容を表示することです。スクリーンショットは grep では確認できないため、
  公開前に目視で確認します。リリースのチェックリストにもそう明記しました。

- これまで文書化されていなかった 9 つの画面を追加しました。アップグレードの流れ、
  インベントリ検索、ルールパックとその詳細表示、ファイル名別のルール一覧、
  そして統計です。GitHub Pages のギャラリーには 19 点すべてを掲載しています。

## v1.6.10 (2026-08-31)

- **`tools/preflight.py`: リリースのチェックリストが自分で走るようになりました。**
  その中の各項目は、いずれも一度は見落とされたことがあるから存在します。
  両方の README と Pages サイトのバージョンバッジは、チェックリストに明記されて
  いたにもかかわらず 9 リリースにわたって 1.6.0 のままでしたし、実在するホスト名と
  クライアントアドレスを含むデコーダーのコメントが、まさにそれを禁じる規定が
  あったにもかかわらず、公開パックとして出荷済みでした。どちらも機械的に
  検出されるようになりました。

  確認するのは、バージョンとバッジの整合、現行バージョンの変更履歴の項目、
  `github/` が作業ツリーからずれていないこと、公開物がプライベートアドレスや
  内部ホスト名を含まないこと、パックのマニフェストのハッシュとルール ID の範囲、
  `INDEX` の整合、ルール説明が英語のみであること、i18n 辞書が実際に埋め込まれて
  いること、テンプレートの JavaScript が解析できること、README が参照する
  スクリーンショットがすべて存在すること、そして相関ルールがレベル 0 の親に
  ぶら下がっていないこと —— それは決して発火しないからです。

- プライバシー判定のパターンはテストで固定しています。静かに一致しなくなった
  チェックは、開いたまま失敗するからです。それらのテストは、最初にこの仕組みを
  壊したケースも扱っています。`10.1.20.1762506875` という名前の Zimbra の jar は
  バージョンであって、アドレスではありません。

- 新しいチェックが見つけたものを修正しました。古いバージョンバッジ、AdGuard の
  デコーダーのコメントに残っていた実在のホスト名とクライアント IP、
  ソースコードのコメントにあった内部ホスト名、そして作業ツリーの
  `install.sh`／`uninstall.sh` に残っていた改名前の製品名です。

## v1.6.9 (2026-08-31)

- **ルール階層の検索がファイル名を受け付けるようになりました。** これまでは
  ルール ID しか取らず、「このルールに関連するものは何か」には答えられても
  「このファイルには実際に何が入っているか」には答えられませんでした。
  後者は、パックを確認するときや単一のルールセットファイルを監査するときに
  問われることです。数字以外の入力はファイル名の断片として扱われるようになり、
  `zenarmor`、`ZENARMOR`、`zenarmor-rule`、`zenarmor-rule.xml` はいずれも同じ
  ファイルを見つけます。一致したファイルはツリーの根として現れ、その下に
  親子関係でルールが入れ子になります。

  ファイル名になり得ない入力 —— パス区切りを含むもの、64 文字を超えるもの ——
  は検索せずに拒否します。値は、すでにディスクから読み込まれた名前に対して
  部分一致で照合されるだけで、ファイルシステムそのものへ到達することはありません。

## v1.6.8 (2026-08-31)

- **AdGuard: フィルター回避のブロックがアラートになり、継続的なものは要約されます。**
  バイパスのルールはレベル 3 のままです —— ブロックは「制御が働いた」ことであり、
  運用者はその一件一件を見たいからです。その裏にある件数は現実的なもので、
  Apple と Chrome の 4 台の端末が同じ 5 つの暗号化 DNS エンドポイントに対して
  1 日で 152 件のブロックを生みました。そこで新しい相関ルールが、
  そのパターンを目視で見つけさせるのではなく、クライアントごとに 1 時間に
  1 度報告します。

## v1.6.7 (2026-08-31)

- **パックの名称と説明が、読み手の言語に従うようになりました。** ルールパックの
  表とその詳細ダイアログは `name_zh` と `summary_zh` を無条件に表示していたため、
  英語話者の運用者がタブを開くと、パックのカタログ全体が中国語で書かれていました。
  どちらも現在の UI 言語で選択するようになり、パックの注意書きにすでに適用済みの
  修正と揃いました。コードではなくスクリーンショットを見て気づいたものです。

- インベントリとルールパックのタブのスクリーンショットを追加しました。
  これらは 1.5.0 と 1.6.0 以来、文書化されないまま出荷されていました。
  実環境ではなくモック化した Wazuh API に対して撮影しているため、公開画像には
  実在のものではなくデモ用のホスト名とアドレスが写っています。

## v1.6.6 (2026-08-30)

- **Zimbra のベースラインを実サーバーから作り直しました。** `zimbra-webfiles` は、
  どのファイルが標準構成のものかを FIM のルールへ伝えるリストです。これがずれて
  いました。直近 7 日間のアラートに対して計測したところ、新しいベースラインと
  1 つの新規抑制により、Zimbra のファイル整合性に関する誤検知の 78% が解消されます。

  その背後には実際の抜けが 2 つありました。ベースラインには `/opt/zimbra/lib/ext`
  の項目が 1 件もなく、そこではルール 100806 がレベル 15 で発火します ——
  つまり標準構成の拡張 jar 34 個はすべて、1 回変更されれば「メールサーバーの
  パスワードが盗まれている」と主張する CRITICAL アラートになる状態でした。
  さらにベースラインは `*.jar` だけから作られていた一方、100806 はそのディレクトリ内の
  *あらゆる*ファイルでアラートを出すため、同梱されている `.wsdl`、`.xsd`、
  `.properties` は未登録のままでした。

  追加したファイルはすべて、信頼する前に確認しています。所有者、記録された
  アップグレード日時に対する更新時刻、そして唯一の JSP については両サーバーで
  SHA256 が一致することと、ウェブシェルの兆候がないことの走査です。

- **パッケージ管理の一時ファイルがアラートにならなくなりました。** dpkg、ucf、rpm は
  新しい内容を `.dpkg-new` のような名前で書き、数秒後にその場へ改名します。
  そうした名前がベースラインに載ることはあり得ませんし、実際にインストールされる
  ファイルは最終的な名前で引き続きアラートになります。Zimbra の FIM ルールと
  永続化のルールの両方で抑制しました。これにより、`apt-compat.dpkg-new` が
  すべてのホストで出していたレベル 13 の「スケジュールタスクが変更された」
  アラートも解決します。

- Zimbra パックの注意書きに、正しい再生成コマンドを記載しました。また、
  アップグレードのたびにベースラインを作り直す必要があることを明記しました ——
  Zimbra のアップグレードはバージョン名の入った jar を置き換えるため、
  新しいファイル名はどれも古いベースラインにとって未知だからです。

## v1.6.5 (2026-08-30)

- **新しいパック: AdGuard Home。** AdGuard のクエリログ向けのデコーダーとルールで、
  ルール ID は 130900〜130999 を使います。

  このパックは、DNS ベースのあらゆる検知に対する「誰がやったか」の層です。
  再帰リゾルバーやファイアウォールは、クエリの送信元としてリゾルバー自身の
  アドレスしか見られないため、悪性の名前解決をホストまで辿れません。
  実ネットワークでの計測では、DNS トラフィックの 17% が AdGuard を経由して
  リゾルバーへ到達しており、そのため帰属不能でした。最も疑わしい活動を
  生成していたクライアントもその中に含まれ、リゾルバーのログにはまったく
  現れていませんでした。

  通常の広告・トラッカーのブロックは沈黙したままです。直接表に出るのは 2 つだけ ——
  脅威向けのフィルターリストによってブロックされた名前解決と、DoH や Private Relay
  のようなフィルター回避リストによってブロックされた名前解決です。単一クライアントの
  件数は相関に委ねます。ブロック 1 件には意味がなく、400 件あればそのホストは
  調べる価値があるからです。

- 基礎ルールはレベル 0 ではなくレベル 2 に置いており、パックの注意書きにその理由を
  記しています。`if_matched_sid` はレベル 0 のルールに対してまったく発火しません。
  直接計測しました —— 条件に合う 45 件のイベントを送っても、レベルを変えるまで
  頻度ルールは一度も発火しませんでした。レベル 2 はアラートの閾値より下なので、
  沈黙を保ちつつ相関には使えます。他のパックの既存の相関ルールもこの観点で
  すべて監査し、35 件はいずれも問題ありませんでした。

## v1.6.4 (2026-08-30)

- **パックが、動作するために何を必要とするかを明示するようになりました。**
  同梱されていたルールのいくつかは決して発火し得ず、そのことを知らせるものも
  ありませんでした。906121 と 906122 は Sysmon のイベント ID 15 を必要とし、
  906142 と 100990 はプレースホルダー 1 行だけを含む状態で同梱される CDB リストに
  一致し、906120 は syscheck が `C:\Users` を監視していることを必要とします。
  それらのルールがインストール済みと表示されるダッシュボードは、
  存在しない網羅を示していました。各マニフェストは現在、これらを前提条件として
  明記しています。

- **パックの注意書きが読み手の言語で表示されるようになりました。** 詳細パネルは
  `notes_zh` しか表示していなかったため、英語話者の運用者には中国語か、
  さもなくば何も見えませんでした。各マニフェストは英語の `notes` 一覧も持つように
  なり、パネルは現在の UI 言語に合うものを選びます。

- **Zenarmor: パークドメインへの一致をレベル 0 へ下げました。** このカテゴリは
  過剰に反応することが分かりました —— あるクライアント 1 台が 1 日でおよそ 2,900 件の
  アラートを生み、それはパック全体の件数の 88% にあたりました。しかもすべて、
  頂点ドメインに単に A レコードが無いだけの、CDN 経由のアプリケーション
  バックエンドに対するものでした。本物の DGA 活動は Zenarmor 自身の
  Botnet DGA Domains タグがすでに扱っているため、失われるものはありません。

## v1.6.3 (2026-08-30)

- **すべてのパックで、ルールの説明文を英語にしました。** 同梱されていた 179 の
  ルールのうち 128 が繁體中文で自身を説明しており、そのためアラートの文言は
  作者自身のコンソールの外にいる人には読めませんでした ——
  そしてこれらのパックは一般に公開されているものです。説明文はすべて英語になり、
  `$(field)` のプレースホルダーと `[CRITICAL]` / `[HIGH]` / `[WARN]` の接頭辞は
  変えていないため、既存のダッシュボードや下流のパーサーはそのまま動きます。

  句読点は全体を半角に統一しました。紛れ込んでいた全角のコロンと
  全角ダッシュはなくなりました。

- パックのバージョンもそれに応じて更新しました: jt-zimbra 2.1、
  jt-portable-detect 1.3、jt-zenarmor 1.2。各マニフェストの
  `name_zh` / `summary_zh` / `notes_zh` という二言語フィールドは影響を受けません ——
  それらは意図的に二言語のものであり、UI は引き続き中国語のパック名と
  注意書きを表示します。

## v1.6.2 (2026-08-30)

- **Zenarmor パックを実トラフィックに対して作り直しました。** 最初の版は分類を
  すべて `security_tags` に依存させていましたが、それでは本物の信号を取りこぼすことが
  分かりました。Zenarmor は `security_tags` と `category` を**交互に**出力します ——
  一方が存在するときもう一方は null になります —— そのためリスクイベントの半分が
  レベル 0 まで落ち、何にもアラートを出していませんでした。現在は両方のフィールドを
  照合します。

  コンテンツの分類は、弱い信号として相応に格付けしています。Proxy と
  Parked Domains はレベル 5 と 3 です。`is_blocked` はたいてい脅威ではなく
  閲覧ポリシーによるブロックを意味するからです。本物の脅威タグは元の格付けを保ち、
  2 つ目の相関ルールが、ブロックされているにもかかわらず悪性の接続を試み続ける
  ホストを捉えます。

  タグのパターンにある短い語は、単語境界で固定するようにしました。それがないと、
  大文字小文字を区別しない照合で `Tor` が `Torrent` に一致し、
  ごく普通の P2P トラフィックを匿名化ツールの使用として報告してしまいます。

## v1.6.1 (2026-08-30)

- **新しいパック: Zenarmor (OPNsense)。** syslog で転送される Zenarmor NGFW の
  イベント向けに、デコーダーと重大度で格付けしたルールを用意しました。
  ルール ID は 130800〜130899 です。

  アラートは脅威の分類**と**、ファイアウォールが実際に接続をブロックしたかどうかの
  両方で格付けします。そこには意図的な判断があります ——
  **ブロックされなかった悪性の接続は、ブロックされたものより上位**です。
  ブロックされなかったのなら通信は成立しており、そのホストはすでに侵害されている
  可能性があるからです。通り抜けたマルウェア、ボットネット、C2、ランサムウェアの
  トラフィックはレベル 12 を出します。同じトラフィックがブロックされた場合は
  レベル 7 です。フィッシング、クリプトマイニング、DGA は 10 と 5 で続きます。
  ハッキングツール、プロキシ、Tor、既知の侵害済みホストは 7 です。
  1 台の内部ホストから 5 分以内に 6 件のブロックされなかった悪性接続があれば
  レベル 13 になります。

  このパックは意図的に**包括ルールを同梱していません**。分類されないトラフィックは
  レベル 0 のままでアラートにならないため、通常の閲覧がコンソールを溢れさせることは
  ありません。日常的なポリシーによるブロックは、Wazuh へ複製するより
  Zenarmor 自身のレポートで読むほうが適しています。

- パックがデコーダーを第一級のファイル種別として同梱できるようになりました。
  ルールやリストと同じバックアップ・検証・ロールバックを伴って
  `etc/decoders/` へインストールされます。
- 同梱パックのカタログをテストで検証するようになりました。マニフェストのハッシュ、
  インストール先、XML の妥当性、パック間でのルール ID の一意性、`INDEX` の整合を
  すべて確認するため、マニフェストのハッシュ更新を忘れたルール編集は、
  アンインストール時に表面化するのではなくビルドで失敗します。

## v1.6.0 (2026-08-30)

- **新しい「ルールパック」タブ。** Jason Tools が保守する検知ルール群のカタログを、
  ルールタブのもう 1 つの読み取り専用ビューではなく、パッケージマネージャーとして
  提示します。ルールの導入と削除は固有の状態を持つライフサイクル操作なので、
  独立したタブに置いています。

  各パックはマニフェストのもとにルール・デコーダー・CDB リストをまとめています。
  パックを開けば、どのファイルをインストールし、どこへ置き、どのルール ID を使い、
  動作に何が必要かの注意書きまで確認できます。インストールは意図的に慎重です。
  すでにインストール済みのルールとのルール ID 衝突は検出され、強制しない限り
  拒否されます。上書きされるファイルはすべて事前にバックアップされ、
  その後 `wazuh-analysisd -t` でルールセットを検証し、**いずれかが失敗すれば
  インストール全体をロールバック**します —— ファイルもリストの宣言もすべてです。
  中途半端に適用されたまま残ることはありません。

  削除は、パックが置き換えたものを復元します。インストール済みのファイルを
  編集していた場合、削除は停止して、どのファイルがそれにあたるかを伝えます。
  あなたの作業を黙って捨てることはありません。

  最初に同梱するのは 4 つのパックです: 可搬実行ファイルの検知
  （Windows／Linux／macOS）、IP 脅威インテリジェンス、マルウェアのハッシュ照合、
  そして Zimbra の検知一式。

- パックのパスは `/var/ossec` を前提にするのではなく、`config.yaml` の
  `wazuh_path` に従うようになりました。
- `install.sh` がパックのカタログも取得するようになりました。

## v1.5.2 (2026-08-29)

- **ノード設定のずれの検出。** `ossec.conf` は Wazuh クラスタが唯一同期*しない*
  ものなので、あるワーカーが `<list>` の宣言を静かに欠いたまま、それを使う
  すべてのルールを無視していることがあり得ます —— しかも Wazuh にはそのずれを
  表に出すものが何もありません。`GET /api/nodes/config-diff` は各ノードの設定を
  API 経由で読み取り、実際に検知を変えるセクション（`ruleset`、`wodle`、
  `syscheck`、`rootcheck`、`localfile`、`active-response`、`command`）を
  マスターと比較し、そのノードに欠けているものと、そこにしか存在しないものを
  報告します。ノードタブに **Config Diff** ボタンを追加しました。
  比較は文字列ではなく意味に基づくため、コメントや書式の違いがノイズになりません。
  稼働中のクラスタに対して実行したところ、ただちに 4 件の実際のずれが見つかりました。
  その中には、アクティブレスポンスのコマンドをすべて欠いているワーカーと、
  `realtime` なしで FIM を動かしているワーカーが含まれていました。

## v1.5.1 (2026-08-29)

- **ルールタブから、クラスタ全体のルールセットを再読み込みできるようになりました。**
  マスターでルールを編集しても、それだけではワーカー上で有効になりません。
  クラスタはファイルを同期しますが、各ノードの `analysisd` は再読み込みを
  指示されるまで自分のメモリー上のルールセットを保持し続けます。そのため、
  ルールの修正が反映されたように見えて、実際にそれらのエージェントを処理している
  ノードは古いルールのまま動いていることがあります ——
  本番クラスタで起きたのがまさにこれで、修正済みのルールが 4 時間にわたって
  誤検知を出し続けました。再読み込みしていたのはマスターだけだったからです。
  `POST /api/cluster/reload-ruleset` は 1 回の呼び出しで全ノードを再読み込みし
  （`nodes_list` を付けない `PUT /cluster/analysisd/reload`）、単体構成の
  マネージャーでは `PUT /manager/analysisd/reload` へフォールバックします。
  結果とルールセットの警告をノードごとに報告します。`analysisd` に対して直接
  検証しました —— これはホットリロードであり、プロセスは再起動されません。

## v1.5.0 (2026-08-26)

Wazuh Dashboard に無い、あるいは埋もれている 11 の機能。
4.14.7 の API 全 150 エンドポイントを、本ツールがすでに使っているものと
照らし合わせて選定しました。

**新しいインベントリタブ**
- `GET /api/inventory/search` は、syscollector の 1 カテゴリをすべてのエージェントに
  対して並列に照会し、1 つの表として返します。パッケージ、開放ポート、プロセス、
  サービス、ローカルユーザー、修正プログラム、ネットワークインターフェース、OS、
  ブラウザー拡張機能。「このパッケージが入っている／このポートが開いている
  エージェントはどれか」に答えます。Dashboard のエージェント単位のインベントリでは
  できないことです。結果は CSV へエクスポートできます。

**ルールタブがルールセットの作業台になります**
- **ログテスト** —— ログ行を貼り付けると、一致するルールとデコーダー、
  抽出されたフィールド、analysisd のメッセージが表示されます（`PUT /logtest`）。
- **デコーダー** —— デコーダーを閲覧・検索し、その XML を表示します。
- **CDB リスト** —— CDB リストの一覧、作成、編集、削除。

**より安全な設定変更**
- ノードを再起動する前に `ossec.conf` を**検証**します。
- **ルールセットの再読み込み**により、マネージャーを再起動せずにルールの変更を
  適用します。

**エージェント**
- **カスタム WPK アップグレード** —— すでにマネージャー上にある WPK からアップグレード
  します。閉域環境で機能する唯一の経路です。
- **アクティブレスポンス** —— 選択したエージェント上でコマンドを実行します。
- **エージェントの登録** —— 名前で事前登録し、ID とキーを受け取ります。
- **稼働中の設定** —— エージェントが実際に適用した設定です。グループの
  `agent.conf` の変更を、エージェント側で確認できます。
- **エージェントキー** —— 壊れたエージェントを再登録するための登録キーを取得します。

**ノード／グループ**
- **稼働状況** —— ノードごとの analysisd / remoted / wazuh-db のカウンター。
- **ファイル** —— グループディレクトリ内の全ファイルを閲覧・表示します。
  agent.conf だけではありません。

**そのほか**
- 上記すべてを対象とする zh-TW の辞書項目 127 件と規則式 57 件を追加。
- テストは 85 件になりました。
- OWASP ZAP のベースラインは 1.4.3 から変化なし: 失敗 0、警告 8、合格 59。

## v1.4.3 (2026-08-26)

**セキュリティの堅牢化**（OWASP ZAP のベースラインスキャンで検証: 警告 12 → 8、
失敗 0。残る Medium は本質的な `unsafe-inline` の CSP 項目です）:

- すべての応答に**セキュリティ応答ヘッダー**を付与: `Content-Security-Policy`、
  `X-Content-Type-Options`、`X-Frame-Options: DENY`、`Referrer-Policy`、
  `Permissions-Policy`、`Cross-Origin-Opener-Policy`、
  `Cross-Origin-Resource-Policy`、`Cache-Control: no-store`、
  そして HTTPS で動作している場合は HSTS。
- cdnjs から読み込む CodeMirror の資産に**サブリソース完全性 (SRI)** を付与。
  これまでは完全性の検証なしに取得しており、CDN の応答が改ざんされていれば、
  Wazuh API のトークンを保持するコンソールの中で任意の JavaScript が
  実行され得ました。
- **ログインフォームに CSRF トークン。** JSON API は `SameSite=Lax` で保護されて
  いましたが、ログインフォーム自体にはトークンがありませんでした。
- **WSGI サーバーが `Server` ヘッダーでバージョンを広告しなくなりました。**

**ルールタブ**

- **新機能: すべてのルールの XML 全文に対するキーワード検索**
  （`GET /api/rules/search`）。空白区切りの複数キーワードと、
  すべて／いずれかの選択に対応します。表のフィルターは
  id／level／description／file／groups しか見ていなかったため、
  `<field>`、`<regex>`、`<decoded_as>`、`<options>` の中にある語は
  見つけられませんでした。生のテキストを grep するため、XML パーサーが
  拒否するファイル内のルールも見つかります。
- **修正: 階層ビューがスクロールできませんでした。** 自身の寸法指定がなかったため
  `.rules-content` の `flex:1; overflow:auto` が適用されず、高いルールツリーは
  スクロールバーなしでパネルに切り取られていました。

**エージェントタブ**

- **新機能: 選択解除ボタン**を選択件数の横に追加しました。各行のチェックを
  外していかなくても、一括選択をまとめて解除できます。

**翻訳**

- zh-TW の網羅を大きく広げました。テンプレート内の利用者に見えるすべての文字列を
  辞書と突き合わせて洗い出したものです。エージェントのアップグレードの流れ全体
  （ダイアログ、オプション、進捗表、状態の値、集計件数）、確認のプロンプト、
  そして値を含むモーダルのタイトル。辞書項目 40 件以上と正規表現パターン 38 件を
  追加しました。「Found N related rules」もその 1 つです。
- 「Upgrade to manager version」のラベルは構造を変え、そのテキストノードの末尾に
  `(` が含まれないようにしました。それが翻訳不能の原因でした。

**テスト**

- **本プロジェクトにテストがつきました**（`tests/test_web_ui.py`、46 件）。
  `python3 -m unittest discover -s tests` で実行します。完全にオフラインで動作し ——
  Wazuh API はモック化され、ルールセットも偽物です ——
  認証、リクエストの検証、ルール系エンドポイント、セキュリティヘッダー、CSRF、
  反射型 XSS のエスケープ、SRI、バージョン比較器、翻訳の整合を扱います。

## v1.4.2 (2026-08-24)

- **ルールタブが、解析できないルールファイルを報告するようになりました。**
  `parse_rule_file()` は XML のエラーを握りつぶしていたため、不正なファイル内の
  ルールはタブから静かに消えており、何かがおかしいという手がかりもありませんでした。
  `GET /api/rules` は `parse_errors` の一覧を返すようになり、タブは各ファイル名と
  パーサーの理由を示すモーダルへのリンクを伴う警告を表示します。
  （Wazuh 4.14.7 自身がそのようなファイルを 1 つ同梱しています:
  `0910-ms-exchange-proxylogon_rules.xml` の `pcre2` 正規表現には `\<` と `\>` が
  含まれており、ElementTree は 57 行目でこれを拒否し、そのルールを隠します。）
  解析の失敗は DEBUG ではなく WARNING で記録するようになりました。

## v1.4.1 (2026-08-24)

保守リリース。**Wazuh 4.14.7** に対して検証しました —— API と CLI の変更は
不要でした。本ツールが使用するエンドポイントとパスはすべて健在で、
非推奨にもなっていません。

- **修正: ルール階層がルールタブをクラッシュさせることがありました。**
  `find_children()` は深さの上限なく再帰していたため、長い `if_group` /
  `if_sid` の連鎖で `RecursionError: maximum recursion depth exceeded` が発生し、
  リクエストは 500 で失敗しました。現在は降下に上限を設けています
  （打ち切った場合はログに記録します）。
- **修正: JSON 本文のないリクエストが 500 を返していました。**
  呼び出し側が本文を送らない、あるいは JSON ではない `Content-Type` を送ると、
  `request.get_json()` は werkzeug の `BadRequest` を送出します。各ルートの
  汎用ハンドラーはそれをサーバーエラーとして報告していました。
  20 のルートが `get_json(silent=True)` を使うようになりました。
- **追加: 一括エージェント操作の入力検証。** `restart`、`reconnect`、`delete`、
  `upgrade` は `agent_ids` が無い、または空の場合に拒否し、すべての ID を
  検証するようになりました。空のリストで Wazuh API を呼ぶことはありません。
- **追加: `POST /api/groups` がグループ名を検証**するようになりました
  （`DELETE` のルートはすでに行っていました）。不正な名前や名前の欠落は、
  文字どおり `None` という名前のグループではなく 400 になります。
- **修正: バージョン比較が 3 か所で定義されており**、実行時に実際に使われる定義が、
  最初に書かれたものと異なっていました。数値による単一の比較器へ統合したため、
  `4.14.7` は正しく `4.9.0` より上位になります。
- **不要コードの削除:** ロール一覧のための `wazuh-user` CLI フォールバック
  （Wazuh はそのようなバイナリを同梱していません —— RBAC は API 専用です）と、
  未使用のインポート 5 件。
- 新しい検証メッセージの zh-TW 文字列を追加しました。

## v1.4.0 (2026-06-11)
- **二言語 UI（English / 繁體中文）**: ヘッダーのワンクリックで言語を切り替えられ、選択はブラウザーごとに記憶されます。翻訳はすべてクライアント側で適用され（テキストノードと `placeholder`／`title`）、`MutationObserver` が動的に描画された内容も翻訳し続けます。UI の文字列は `lib/i18n_engine.js` にあり、`tools/build_i18n.py` によって `lib/web_ui.py` へ埋め込まれます。
- **独立したリポジトリ**: プロジェクト名を `jt_wazuh_agent_mgr` から **`jt-wazuh-mgr`** へ改め、独自リポジトリ `jasoncheng7115/jt-wazuh-mgr` へ分離しました。インストール先は `/opt/jt-wazuh-mgr`、systemd サービスは `jt-wazuh-mgr` になります。
- **ワンライナーでのインストール／アップグレード/アンインストール**: 堅牢化した `install.sh`（冪等な更新モード）と、きれいに削除するための新しい `uninstall.sh`。
- **ライセンス**: **Apache-2.0** へ変更しました。

## v1.3.136 (2026-03-18)
- **すべてのルールの閲覧**: ルールタブに「すべてのルール」モードを追加
  - 組み込みとカスタムのすべてのルールを、ソート・ページ送りできる表で一覧
  - ルール ID、レベル、説明、ファイル、グループを横断して検索
  - 絞り込み: レベル範囲（最小／最大）、ファイル、種類（カスタム／組み込み）
  - ルール ID をクリックすると階層表示へ移動
  - 新しい API エンドポイント `GET /api/rules`

## v1.3.135 (2026-02-25)
- **メール通知の保存時検証**: 保存する前に、ルール ID・グループ・レベルが Wazuh のルールセットに存在するかを検証します
- **複数 ossec_config への対応**: `<ossec_config>` セクションが複数ある構成（ワーカーノードでよくあります）でのメール通知同期を修正しました

## v1.3.134 (2026-02-24)
- **メール通知の視覚的な管理**: マスターノードの ossec.conf にある `<email_alerts>` ルールをフォームで管理します
- **全ワーカーへの同期**: メール通知の設定をワンクリックですべてのワーカーノードへ同期します（自動バックアップ付き）
- 変更するのは `<email_alerts>` ブロックのみ —— 他の設定内容、コメント、書式は保持されます

## v1.3.133 (2026-02-23)
- **Web セッションの延長**: セッションのタイムアウトは既定で 2 時間になりました（`web.session_timeout` で変更できます）
- **JWT トークンの自動更新**: 期限切れの前に自動で再認証するため、セッション中に再ログインする必要はありません
- **設定画面に SSH セットアップガイド**: SSH 設定の手引きへすぐアクセスできます

## v1.3.131 (2026-02-22)
- **Queue DB の一括クリア**: エージェントを選んで queue DB ファイルを一括削除し、エージェントを自動で再起動します
- **正確なノード指定**: 実際に queue DB ファイルを持つノードにのみ接続します
- **詳細な結果表示**: エージェント別・ノード別の削除結果と再起動状況をモーダルで表示します
- **インストーラーの改善**: インストール時に systemd サービスを自動導入して起動し、更新時は自動再起動、`install.sh` 自身も更新します

## v1.3.10x (2026-01-04)
- **分布バー**: エージェント一覧の上に視覚的な統計バーを追加（状態、OS、バージョン、グループ、ノード、同期状態）。セグメントをクリックすると素早く絞り込めます。左から右へのアニメーションとスライドイン付き
- **統計の自動更新**: 上部の統計を 10 秒ごとに自動更新します
- 分布バーの見た目、位置合わせ、アニメーションに関する各種修正

## v1.3.3x (2026-01-02)
- **新しいルールタブ**: ルール階層ビューアー —— ルール ID で検索、親子関係（`if_sid`、`if_matched_sid`）、折りたたみ可能なツリー、XML のシンタックスハイライト、すべて展開／折りたたみ
- 組み込み（`/var/ossec/ruleset/rules/`）とカスタム（`/var/ossec/etc/rules/`）の両方に対応

## v1.3.2x (2025-01-01)
- **セキュリティの堅牢化**: 入力検証器（`validate_node_name`、`validate_agent_id`、`validate_group_name`、`validate_username`）、パスのホワイトリスト（`validate_path`、`ALLOWED_PATHS`）、シェル引数のエスケープ（`safe_shell_arg` / `shlex.quote`）、ログのサニタイズ、アップロード時の `secure_filename()`、パストラバーサル対策の `ALLOWED_SYNC_ITEMS` ホワイトリスト
- **統計列のソート**とセマンティックバージョンによる並べ替え
- **同期状態**の見た目と読み込み表示の改善、**ファビコン**と `/images/` 静的ルート

## v1.2.x
- エージェントのアップグレード機能、アップグレードファイルの管理、進捗の追跡

## v1.1.x (2024-12-31)
- **エージェント表の列のカスタマイズ**（列の表示／非表示、localStorage に保存）
- SSH による **Queue DB の複数ノード対応**、SSH 経由でのノードのダウンロードと再起動
- **幅のレスポンシブ対応**、24 時間表記への統一、同期フィルターの修正

## v1.0.8x – v1.0.9x (2024-12-31)
- ワーカーノードの SSH リモート管理（ossec.conf の読み書き、サービスの再起動、cluster.key のダウンロード）
- クリップボードへのコピーが付いた SSH セットアップガイド、設定モーダル（API 接続、SSH の状態、このツールについて）
- ノード検出、バージョン解析、JavaScript の正規表現エスケープに関する多数の修正

> 1.4.0 より前の、より細かい完全な履歴については、プロジェクトの以前のリリースノートをご覧ください。
