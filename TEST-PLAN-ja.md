# テスト計画

[English](TEST-PLAN.md) | [繁體中文](TEST-PLAN-zh-TW.md) | [日本語](TEST-PLAN-ja.md)

リリース前に何を、どの層で検証しているか。そして —— できるかぎり正直に述べて ——
何をまだ検証できていないか。

計画を層に分けているのは、層ごとに捕まえられる誤りが違うからです。
自動テストは「守らなくなったガード」を捕まえます。機械的なチェックは
「誰も更新しなかったバージョンバッジ」を捕まえます。スキャンは「消えたヘッダー」を
捕まえます。ブラウザーのチェックリストは、人が画面を見て初めて存在する事柄を捕まえます。
どれも互いの代わりにはなりません。

---

## すべてを実行する

```bash
python3 -m unittest discover -s tests     # 186 件のテスト、完全にオフライン
python3 tools/preflight.py                # 21 項目の機械的なリリース前チェック

# システムに Flask が入っていないホストで
python3 -m pip install --no-index --find-links=offline_packages \
        --target=/tmp/vendor flask requests pyyaml
PYTHONPATH=/tmp/vendor python3 -m unittest discover -s tests
```

テストに Wazuh Manager は不要です。API 層はモック化され、ルールセットの
ディレクトリも合成されたものを使います。

---

## 第 1 層 —— 自動テスト

`tests/test_web_ui.py` が読み取り系の経路とセキュリティ特性を、
`tests/test_ui_operations.py` が状態を変更する経路を担当します。

| 領域 | 検証内容 |
|---|---|
| 認証 | アプリの `url_map` にあるすべてのルートについて、未認証の呼び出しを確認します。手書きの一覧ではなくルートマップから導出しているため、後から追加されたルートも誰かが覚えていなくても対象になります。セッション期限切れは `session_expired` を返します。 |
| セキュリティヘッダー | CSP、`X-Content-Type-Options`、`X-Frame-Options`、リファラーポリシー、`Server` にバージョンを出さないこと。 |
| サプライチェーン | CDN の `<script>` と `<link>` がすべて SRI ハッシュを持つこと。 |
| CSRF | ログインフォームがトークンを発行し、必須としていること。 |
| 反射型 XSS | テンプレートへ出力される値がエスケープされること。 |
| 入力検証 | エージェント ID、グループ名、ノード名、ファイル名、アクティブレスポンスのコマンドと引数。名前としての `..`、シェルのメタ文字、長すぎる値を含みます。 |
| 破壊的なルート | Queue DB のクリア、グループ所属の変更、アップグレードファイルの削除は、**拒否される経路のみ**を実行します。リストが来るべき場所に文字列が渡されたとき、1 文字ずつ反復してはいけません。 |
| パスの封じ込め | グループファイルの読み取り、ノードのファイルダウンロード（パスではなくキーワード）、WPK の削除、パックのマニフェストが指す配置先。 |
| 設定の編集 | 空・過大・不正な本文を拒否すること。XML 外部実体を解決しないこと。 |
| ルール | 階層、一覧、内容検索、ファイル名検索、ElementTree が解析できない不正なルールファイル。 |
| デコーダー、CDB リスト | 一覧、読み取り、CDB への書き込み経路。 |
| ログテスト | リクエストの形式とレスポンスの解析。 |
| インベントリ | エージェント横断のパッケージ検索。 |
| クラスタ | ノード全体のルールセット再読み込み、ノード設定の差分。CDB リストの宣言が各ノードへ届くこと: 宣言テキストそのもの、対向ノードへの書き込み経路、書き込みを受理しても適用しないノード、重複した宣言が二重に書かれないこと、到達できないノードがあるときの中断とロールバック、および `local_only` の上書き。 |
| パック | マニフェストの整合性、同梱ファイルすべての SHA-256、ルール ID の一意性、`dest` パス、`packs/INDEX` の整合、スクリプト／cron／エージェントグループのインストール、そしてどのパックにも内部のホスト名やアドレスが含まれないこと。 |
| フロントエンド | テンプレートの JavaScript が `node --check` を通ること。i18n 辞書が埋め込み済みで、内部的に整合していること。 |
| バージョン比較 | エージェントのバージョン比較器（プレリリース版の扱いを含む）。 |

**破壊的なルートを拒否経路だけで試験する理由。** それらの多くは、検証を通過した
あとに外部コマンドを実行したりファイルを削除したりします。成功経路まで走らせる
テストは、そのテストを実行したマシンに対して実際に作用します。そして本プロジェクト
では、そのマシンはたいてい稼働中の Wazuh Manager です。主張する価値があるのは
ガードの部分であり、その先は第 5 層で意図的に手作業で検証します。

---

## 第 2 層 —— 機械的なリリース前チェック

`tools/preflight.py`。すべて通過しなければなりません:

1. バージョンとバッジが `lib/__init__.py` と一致すること
2. 各 README の見出しにあるバージョン（バッジではない）
3. 両方の変更履歴に本バージョンの項目があること
4. リリースノートが変更履歴だけに存在し、README に複製されていないこと
5. ライセンスの表記がどこでも同一であること
6. 公開物すべてにおいて、プロジェクト名が `jt-wazuh-mgr` であること
7. アイコン一式が存在し、参照されていること
8. AGPL 第 13 条が求めるとおり、画面からソースコードを提供していること
9. `github/` が作業ツリーを反映していること —— 一覧ではなく、両方に存在する全ファイル
10. 公開物に内部のホスト名・アドレス・メールアカウントが含まれないこと
11. パックのマニフェスト: ハッシュ、ルール ID、`dest` パス、英語の説明文、そして `if_matched_sid` がレベル 0 の親を指していないこと
12. `packs/INDEX` がディスク上のファイルと一致すること
13. `web_ui.py` に埋め込まれた i18n 辞書が最新であること
14. テンプレートの JavaScript が解析できること
15. README が参照するスクリーンショットがすべて存在すること
16. 同梱の CDB リストにプレースホルダーのままのものがないこと
17. ドキュメントに書かれた数がソースと一致すること —— テスト数、チェック数、シナリオ数
18. リリース済みの全バージョンにタグがあること。準備中のバージョンは失敗ではなく警告
19. 各文書の全言語版が互いにリンクしていること
20. 公開時の衛生 —— `wazuh-rules/` を含めない、余計な書庫を置かない、秘密情報を含めない
21. 2 つのパックに重複して存在するパターンが、両方で同一であること

16 番目のチェックが存在するのは、かつて捏造されたハッシュを 1 件だけ持つリストを
参照するレベル 15 のルールを公開してしまったからです。そのルールは決して発火せず、
そのことを知らせるものも何もありませんでした。

17 番と 18 番は、3 つ目の言語を用意している最中に、**このファイルに書かれていた
数字自体が 3 リリース分古くなっていた**ことが判明したため追加されました。

---

## 第 3 層 —— セキュリティスキャン

```bash
PYTHONPATH=/tmp/vendor python3 wazuh_agent_mgr.py --web --host 127.0.0.1 --port 5099 &
/snap/bin/zaproxy -cmd -autorun /絶対パス/plan.yaml
```

plan は ZAP の Automation Framework です。スパイダー、パッシブスキャン、
そして HTML と JSON のレポートを生成します。**パスはすべて絶対パスで**指定してください ——
相対パスは snap 自身のディレクトリを起点に解決され、`Cannot access file` は
ログにしか現れず、終了コードは 0 のままになります。

この構成で計測したベースライン: **Medium 2 件、Informational 5 件、High と Low は 0 件。**
Medium の 2 件はいずれも、スクリプトとスタイルに対する CSP の `unsafe-inline` です。
合計値ではなく、プラグイン単位で比較してください —— 同じ件数でも入れ替わりを
隠していることがあります。新しいプラグイン ID が出たらリリースを中止します。

一見すると不穏な 2 件の Informational は、実際には問題ではありません。
「suspicious comment」の正体は *user* という単語であり、
「user controllable HTML element attribute」はログインフォーム自身の入力欄です。

以前のベースラインだった **0 FAIL / 8 WARN / 59 PASS** は docker イメージの
`zap-baseline.py` によるもので、その PASS 件数に相当するものはここにはありません。
引き継げるのはアラートの一覧のほうです。

Medium が 2 件残っているのは、フロントエンド全体が Python の文字列の中に
インラインで書かれているためです。これを解消するにはテンプレートの構造を
作り直す必要があり、リリース時に行う類の変更ではありません。

---

## 第 4 層 —— ブラウザーシナリオ（自動）

```bash
tests/e2e/run.sh          # 19 シナリオ、81 チェック、docker が必要
```

`tests/e2e/mock_api.py` は、モック化され状態を持つ Wazuh API に対して実際の
アプリケーションを動かします。そのためシナリオは「操作し、その操作の結果を
検証する」ことができます。起動方法も本番と同じにしてあります ——
`app.run()` を単体で呼ぶとヘッダーの堅牢化が飛ばされ、テストは別のプログラムを
測っていることになるからです。実際のルールセットは決して読みません。
ルール系のエンドポイントはパスが `/var/ossec` に固定されており、
これらのテストを実行するマシンはたいてい稼働中のマネージャーだからです。

`tests/e2e/journeys.js` が実際のブラウザーを動かします: ログインと未認証呼び出しの
拒否、各タブの描画、独自の複数選択による絞り込み、エージェントの選択、確認ダイアログ、
グループを作成して一覧に現れること、両クラスタノードとそのデーモン、ID とファイル名
によるルール検索、スケジュールジョブを明示するパックの詳細、ログ、統計、
画面に認証情報が出ないユーザー一覧、インベントリ検索、繁體中文への翻訳で
名前とアドレスが変わらないこと、実レスポンスのセキュリティヘッダー、そして最後に
「静かに失敗したものがないか」の確認 —— 未捕捉のエラーと失敗したリクエストの両方を、
文書化された 1 件の失敗（ワーカーのログ読み取りには任意の SSH 設定が必要）は
許容しつつ、その 1 件は必ず存在することまで確認します。

シナリオ作成時に回避が必要だった点を 1 つ記しておきます。画面はタイマーで自動更新
されるため、ダイアログへ入力してからボタンを押すまでの間に更新が挟まると、
モーダルが再描画されます。少し前に取得したダイアログは、押そうとした時点では
ボタンを失っていました。

## 第 4b 層 —— ブラウザーのチェックリスト（手作業）

自動テストが動かすのは Flask のルートであって DOM ではありません。以下はすべて
人とブラウザーが必要です。エージェントが 1 台以上あるマネージャーに対して、
両方の言語で実施してください。翻訳層は描画後にテキストノードを書き換えるためです。

**ログイン** —— トークン期限のヒントが表示される。セッションのカウントダウンが表示され
減っていく。期限切れでスタックトレースを出さずにログインフォームへ戻る。
誤った認証情報がきれいに失敗する。

**エージェント** —— 一覧が読み込まれる。状態・OS・グループでの絞り込み。検索。
1 件・複数・全件の選択。再起動。削除（確認は 2 段階）。最新版へのアップグレード、
指定バージョンへのアップグレード、アップロードした WPK からのアップグレード。
アップグレード結果の表示。エージェントの詳細。エージェントキー。稼働中の設定。
Queue DB のサイズとクリア。アクティブレスポンスのコマンド。

**グループ** —— 作成。削除。エージェントの追加と削除。全件削除。グループ間の移動。
排他的な割り当て。設定の表示・編集・ダウンロード・インポート。ファイル一覧。

**ノード** —— 一覧と状態。デーモンの統計。サービスの再起動。再接続。
ルールセットの再読み込み。設定の表示・編集・検証・ダウンロード。メール通知の設定。
アップグレードファイルの一覧・アップロード・削除。同期状態とその詳細。ログ。

**ルール** —— 5 つのモードすべて: 階層、すべてのルール、デコーダー、CDB リスト、
ログテスト。内容による検索とファイル名による検索（名前の一部、拡張子付きの名前を含む）。
ファイルの表示。ルールの削除。CDB リストの作成・編集・削除。

**パック** —— 一覧。ファイル、スクリプト、スケジュールジョブ、エージェントグループ、
および CDB リストを宣言できなかったノードを示す詳細。インストール。
ルール ID が衝突する状態でのインストールと、その後の強制実行。削除して
置き換えられていたファイルが戻ることの確認。インストール後に編集されたファイルを
含むパックの削除（拒否されなければなりません）。

**インベントリ** —— エージェント横断のパッケージ検索。

**統計** —— 概要とレポート。

**API ユーザー** —— 一覧、作成、削除、ロールの変更。

**すべての言語** —— 各タブで繁體中文と日本語へ切り替えます。JavaScript が生成した
内容に英語が残っていないか、また英語のままにすべき語が過剰に翻訳されていないかを
確認します。`value` と `<option value>` は決して変わってはいけません。

**オフライン** —— cdnjs へ到達できない状態で、設定エディターはシンタックス
ハイライトを失いますが、読み込み・編集・保存はできなければなりません。

---

## 第 5 層 —— 配備

1. 配備したあと、**配備先のパスそのものから**バージョンを読み戻します。
   `-C` を付けずに展開した tar は別の場所へ展開され、間違った方を読んだ
   バージョン確認は二度続けて「成功」と報告します。
2. サービスを再起動し、起動してくることを確認します。
3. クラスタの場合: ルールをワーカーへ同期し、両ノードを再読み込みし、
   **ワーカー側でも検証**します。クラスタが同期するのは `etc/rules` と
   `etc/lists` であって `ossec.conf` ではなく、ワーカーの analysisd を
   再読み込みすることもありません。
4. `wazuh-analysisd -t` が新しい警告なしで通ること。
5. 発火すべきルールが実際に発火することを確認します。リストの改名やファイルの
   移動では何も止まりません —— ルールは読み込まれ、ただ一度も一致しなくなります。
   再読み込みの応答を読んでください。analysisd は、読み込めなかったリストと
   無視することになるルールを、成功として返る応答の中の警告として示します。

---

## 検証していないこと

はっきり書いておきます。実際より広い網羅を匂わせるテスト計画は、短いものより
たちが悪いからです。

- **DOM レベルの自動化は行っていません。** フロントエンドについて確認しているのは
  構文の妥当性と翻訳の整合だけです。第 4b 層の操作はすべて手作業です。
  スクリーンショット用の仕組みはモック API に対して実ページを描画しており、
  ここを育てていくのが自然な方向です。
- **破壊的なルートの成功経路は CI で実行していません。** 理由は第 1 層に記したとおりです。
- **負荷試験・並行性の試験を行っていません。** エージェントが数千台ある場合や、
  2 人の運用者が同じ設定を同時に編集した場合の挙動は測定されていません。
- **バージョン間のアップグレード試験を行っていません。** 旧リリースへの上書き
  インストールは手作業で確認しています。
- **パックの検知ルールを、サンプルログに対して CI で試験していません。**
  マネージャー上で `wazuh-logtest` を使い、シナリオごとに手作業で検証しています。
  1 つの親の下にある兄弟ルールは予測できない順序で評価されるため、
  単独では通るルールが、実際の配置では他のルールに隠されることがあります。
