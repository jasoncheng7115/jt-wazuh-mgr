<!--
  Zenarmor (OPNsense) 偵測規則
  ID 區間: 130800-130899
  Wazuh 沒有內建 Zenarmor 的規則或解碼器, 全數自行維護.
  搭配解碼器: etc/decoders/zenarmor-decoder.xml (program_name=zenarmor, data= 之後為 JSON)

  === 2026-08-30 依真實資料改版 ===

  實際可用的風險欄位 (以正式環境流量驗證):
    security_tags  威脅標籤. 實測值: ["Malware/Virus"], ["Parked Domains"]
    category       內容分類. 實測值: "Proxy", "Parked Domains"
    is_blocked     0=放行 1=已阻擋 (Zenarmor 政策阻擋, 不等於威脅)
    ip_src_saddr / ip_dst_saddr / dst_hostname / ip_dst_port / ja3 /
    dst_geoip.country_name / device.* / encryption

  重要觀察:
    * security_tags 與 category 在實測中「互斥出現」——
      有標籤時 category 為 null, 有分類時 security_tags 不存在.
      因此兩者都要接, 否則會漏.
    * 同一條連線可能被送多份 (Zenarmor 對 conn / tls / dns 各送一次),
      實測同一次 DNS 查詢產生 4 筆事件. 關聯規則的門檻已把重複計入.
    * is_blocked=1 多半只是「上網政策封鎖」, 不是威脅偵測.
      真正的威脅偵測仍應以 Suricata 與防火牆阻擋為主, 本規則集只作輔助.
    * dst_hostname 在 DNS 事件裡是「被查詢的網域」, 不是連線目的主機;
      ip_dst_port=53 即為此類. 判讀時不要當成已建立的連線.

  分級原則:
    1. 先看威脅標籤, 再依是否被阻擋調整等級.
    2. 未被阻擋的惡意連線最嚴重 —— 沒擋下代表連線已成立, 主機可能已被入侵.
    3. 內容分類 (Proxy / Parked Domains) 只是弱訊號, 一律 level 5 以下.
    4. 各條件互斥, 不依賴規則比對順序 (原因見下).

  === 修改前務必先讀 (2026-08-30 實測, Wazuh 4.14.7) ===

    * 同一父規則的子規則, 評估順序「不可預期」——
      既不是 rule ID 由小到大, 也不是檔案內的順序.
      實測一組 if_sid 相同的子規則, 評估順序為
      130811 -> 130801 -> 130803 -> 130802, 毫無規律.
      後果: 只要存在一條條件較寬鬆的 catch-all, 它可能先命中並中止評估,
      讓具體分類規則永遠沒機會.

    * 因此本檔「不設 catch-all」: 只有帶威脅標籤或風險分類的事件會被分級,
      其餘連線落在 130800 (level 0) 不產生告警.
      一般政策阻擋的記錄請由 Zenarmor 自身的 Reporting 查看.

    * negate="yes" 不可靠: 欄位不存在時不成立, 且搭配 type="pcre2" 時
      即使欄位存在且值不符也不會成立. 一律改用正向比對 (^0$ / ^1$).

    * 短詞一定要加 \b 邊界. 例如 Tor 若不加邊界會誤中 "Torrent",
      Proxy / VPN / DGA 同理. 這是 pcre2 不分大小寫比對下的實際陷阱.

  欄位名稱依官方整合指南:
    https://www.zenarmor.com/docs/guides/integrating-zenarmor-with-wazuh
  官方範例規則全部是 level 3 且一律描述為 "blocked", 無法區分嚴重度.

  改完請用 wazuh-logtest 逐一情境驗證, 叢集環境下 worker 要另外同步並再驗一次.
-->
<group name="zenarmor,">

  <rule id="130800" level="0">
    <match>daemon=zenarmor</match>
    <description>Zenarmor event</description>
    <options>no_full_log</options>
  </rule>

  <!-- ===== 威脅標籤 (security_tags) ===== -->

  <!-- 惡意程式 / 殭屍網路 / C2 / 勒索軟體：未被阻擋 = 連線已成立 -->
  <rule id="130801" level="12">
    <if_sid>130800</if_sid>
    <field name="security_tags" type="pcre2">(?i)(Malware|Virus|Botnet|C&amp;C|Command.?and.?Control|Ransomware|Trojan|Spyware|\bWorm\b)</field>
    <field name="is_blocked">^0$</field>
    <description>[HIGH] Zenarmor: malicious connection was not blocked - $(security_tags) from: $(ip_src_saddr) to: $(dst_hostname) $(ip_dst_saddr)</description>
    <mitre>
      <id>T1071</id>
    </mitre>
    <options>no_full_log</options>
    <group>zenarmor,malware,c2,</group>
  </rule>

  <!-- 同樣的威脅但已被擋下：仍需調查來源主機, 但風險較低 -->
  <rule id="130802" level="7">
    <if_sid>130800</if_sid>
    <field name="security_tags" type="pcre2">(?i)(Malware|Virus|Botnet|C&amp;C|Command.?and.?Control|Ransomware|Trojan|Spyware|\bWorm\b)</field>
    <field name="is_blocked">^1$</field>
    <description>[WARN] Zenarmor: malicious connection blocked - $(security_tags) from: $(ip_src_saddr) to: $(dst_hostname) $(ip_dst_saddr)</description>
    <mitre>
      <id>T1071</id>
    </mitre>
    <options>no_full_log</options>
    <group>zenarmor,malware,blocked,</group>
  </rule>

  <!-- 釣魚 / 挖礦 / 詐騙：未被阻擋 -->
  <rule id="130803" level="10">
    <if_sid>130800</if_sid>
    <field name="security_tags" type="pcre2">(?i)(Phishing|Cryptomining|Crypto.?Mining|\bDGA\b|Scam|Fraud|Spam)</field>
    <field name="is_blocked">^0$</field>
    <description>[WARN] Zenarmor: phishing or cryptomining connection was not blocked - $(security_tags) from: $(ip_src_saddr) to: $(dst_hostname) $(ip_dst_saddr)</description>
    <mitre>
      <id>T1566</id>
    </mitre>
    <options>no_full_log</options>
    <group>zenarmor,phishing,</group>
  </rule>

  <rule id="130804" level="5">
    <if_sid>130800</if_sid>
    <field name="security_tags" type="pcre2">(?i)(Phishing|Cryptomining|Crypto.?Mining|\bDGA\b|Scam|Fraud|Spam)</field>
    <field name="is_blocked">^1$</field>
    <description>Zenarmor: phishing or cryptomining connection blocked - $(security_tags) from: $(ip_src_saddr) to: $(dst_hostname)</description>
    <options>no_full_log</options>
    <group>zenarmor,phishing,blocked,</group>
  </rule>

  <!-- 規避類：代理 / 匿名化 / 已遭入侵主機 -->
  <rule id="130805" level="5">
    <if_sid>130800</if_sid>
    <field name="security_tags" type="pcre2">(?i)(Hacking|Proxy|Anonymizer|\bTor\b|\bVPN\b|Compromised|Suspicious)</field>
    <description>Zenarmor: suspicious or evasive connection - $(security_tags) from: $(ip_src_saddr) to: $(dst_hostname) $(ip_dst_saddr)</description>
    <mitre>
      <id>T1090</id>
    </mitre>
    <options>no_full_log</options>
    <group>zenarmor,evasion,</group>
  </rule>

  <!-- ===== 內容分類 (category) ===== -->
  <!-- security_tags 不存在時, Zenarmor 改以 category 表達, 兩者互斥出現 -->

  <rule id="130806" level="5">
    <if_sid>130800</if_sid>
    <field name="category" type="pcre2">(?i)(Hacking|Proxy|Anonymizer|\bTor\b|\bVPN\b)</field>
    <description>Zenarmor: connection in an evasion category - $(category) from: $(ip_src_saddr) to: $(dst_hostname) $(ip_dst_saddr)</description>
    <mitre>
      <id>T1090</id>
    </mitre>
    <options>no_full_log</options>
    <group>zenarmor,evasion,</group>
  </rule>

  <!-- 寄放網域 (Parked Domains): 過期網域常被重新註冊用於釣魚, 但多數只是廢棄網域.
       弱訊號, 僅作記錄, 不升級. -->
  <rule id="130807" level="3">
    <if_sid>130800</if_sid>
    <field name="security_tags" type="pcre2">(?i)Parked</field>
    <description>Zenarmor: connection to a parked domain - from: $(ip_src_saddr) to: $(dst_hostname) $(ip_dst_saddr)</description>
    <options>no_full_log</options>
    <group>zenarmor,parked,</group>
  </rule>

  <rule id="130808" level="3">
    <if_sid>130800</if_sid>
    <field name="category" type="pcre2">(?i)Parked</field>
    <description>Zenarmor: connection to a parked domain - from: $(ip_src_saddr) to: $(dst_hostname) $(ip_dst_saddr)</description>
    <options>no_full_log</options>
    <group>zenarmor,parked,</group>
  </rule>

  <!-- ===== 關聯：同一來源反覆觸發 ===== -->
  <!-- 門檻已把「同一連線送多份」的重複計入 (實測單次事件最多產生 4 筆) -->

  <rule id="130830" level="13" frequency="6" timeframe="300">
    <if_matched_sid>130801</if_matched_sid>
    <same_field>ip_src_saddr</same_field>
    <description>[HIGH] Zenarmor: one internal host made repeated unblocked malicious connections in a short window, the host may be compromised: $(ip_src_saddr)</description>
    <mitre>
      <id>T1071</id>
    </mitre>
    <options>no_full_log</options>
    <group>zenarmor,malware,c2,</group>
  </rule>

  <!-- 惡意連線雖被擋下, 但同一主機持續嘗試, 代表該主機上有東西在反覆外連 -->
  <rule id="130831" level="10" frequency="12" timeframe="600">
    <if_matched_sid>130802</if_matched_sid>
    <same_field>ip_src_saddr</same_field>
    <description>[WARN] Zenarmor: one internal host keeps retrying malicious connections, all blocked so far, check the host: $(ip_src_saddr)</description>
    <mitre>
      <id>T1071</id>
    </mitre>
    <options>no_full_log</options>
    <group>zenarmor,malware,blocked,</group>
  </rule>

</group>
