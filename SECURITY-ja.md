# セキュリティポリシー

[English](SECURITY.md) | [日本語](SECURITY-ja.md)

## サポート対象バージョン

セキュリティ更新の対象は、**jt-wazuh-mgr** の最新リリースのみです。
問題を報告される前に、必ず最新版でご確認ください。

| バージョン | サポート |
|---------|--------------------|
| 1.7.x   | :white_check_mark: |
| < 1.7   | :x:                |

## 脆弱性の報告

脆弱性を発見された場合は、**公開の GitHub issue を作成しないでください。**
GitHub の[非公開の脆弱性報告](https://github.com/jasoncheng7115/jt-wazuh-mgr/security/advisories/new)
（Security → Report a vulnerability）からご報告いただくか、メンテナー
[@jasoncheng7115](https://github.com/jasoncheng7115) までご連絡ください。

次の内容を添えてください:
- 脆弱性の説明と、その影響
- 再現手順（可能であれば PoC も）
- 影響を受けるバージョン
- 想定される修正案があればその内容

数営業日以内に一次回答を差し上げます。問題が確認できしだい修正を準備してリリースし、
匿名をご希望でない限り、リリースノートにお名前を記載します。

## 適用範囲と配備に関する注意

本ツールは **Wazuh Manager の管理インターフェース**であり、
**Wazuh Manager 本体**（クラスタ構成ではマスターノード）にインストールすることを前提としています。
エージェントの再起動・削除・アップグレードやノード設定の編集ができるため、
特権を持つ管理アプリケーションとして扱ってください:

- **信頼できる管理ネットワーク内でのみ運用してください。** ポート `5000` を
  インターネットへ直接公開しないでください。VPN、ファイアウォール、
  またはアクセス制御を備えたリバースプロキシの背後に配置してください。
- **必ず HTTPS を使用してください。** 同梱の `--ssl-auto` は自己署名証明書を生成します。
  本番環境では自組織の CA が発行した証明書をご利用ください。
- **認証**は Wazuh API に委譲しています。利用者は Wazuh API の認証情報でログインし、
  JWT はサーバー側のセッションに保持されます。Web UI モードでは Wazuh の
  パスワードが `config.yaml` に書き込まれることはありません。
- **総当たり攻撃への対策:** 同一 IP からのログイン失敗が続くと一時的にロックされます
  （既定: 3 回失敗で 30 分）。
- **最小権限:** 運用者に必要な権限だけを持つ専用の Wazuh API ユーザーとロールを
  作成してください（`create_api_user.py` を参照）。

## 組み込みの堅牢化

本アプリケーションには、入力検証とインジェクション対策が組み込まれています:

- エージェント ID、グループ名、ノード名、ユーザー名に対する厳密な検証
- パストラバーサルを防ぐためのパスのホワイトリスト検証
- すべての CLI 呼び出しにおけるシェル引数のエスケープ（`shlex.quote`）
- WPK アップロードにおける安全なファイル処理（`werkzeug.utils.secure_filename`）
- ログインジェクションを防ぐためのログのサニタイズ
- 管理操作の操作ログと監査証跡

## 免責事項

本ソフトウェアは、いかなる保証もなく「現状のまま」提供されます。
破壊的な操作（削除、再起動、アップグレード）を行う前には、重要な設定をバックアップし、
まず本番以外の環境で試してください。ご自身の責任においてご使用ください。
