# 安全政策

[English](SECURITY.md) | [繁體中文](SECURITY-zh-TW.md) | [日本語](SECURITY-ja.md)

## 支援的版本

只有 **jt-wazuh-mgr** 的最新發行版本會收到安全性更新。
回報問題之前，請先確認你執行的是最新版。

| 版本 | 支援狀態 |
|---------|--------------------|
| 1.8.x   | :white_check_mark: |
| < 1.8   | :x:                |

## 回報弱點

若你發現安全性弱點，**請不要開公開的 GitHub issue**。
請改用 GitHub 的[私密弱點回報](https://github.com/jasoncheng7115/jt-wazuh-mgr/security/advisories/new)
（Security → Report a vulnerability），或直接聯繫維護者
[@jasoncheng7115](https://github.com/jasoncheng7115)。

請一併提供：
- 弱點的描述與其影響
- 重現步驟（若可行，附上概念驗證）
- 受影響的版本
- 若有建議的修補方式

我們會在數個工作天內給予初步回覆。問題確認後會準備修補並發行，
除非你希望匿名，否則會在發行說明中具名致謝。

## 適用範圍與部署注意事項

本工具是 **Wazuh Manager 的管理介面**，設計上要安裝在
**Wazuh Manager 本機**（叢集模式則是 Master 節點）。
由於它可以重新啟動、刪除、升級代理程式並編輯節點設定，
請把它視為具有特權的管理應用程式：

- **只在受信任的管理網段上運作。** 不要把連接埠 `5000` 直接暴露到網際網路。
  請將它放在 VPN、防火牆，或具備存取控制的反向代理之後。
- **一律使用 HTTPS。** 內建的 `--ssl-auto` 會產生自簽憑證；
  正式環境請改用你自己 CA 簽發的憑證。
- **認證委派給 Wazuh API** —— 使用者以 Wazuh API 的帳號登入，
  JWT 存放在伺服器端的 session。Web UI 模式下，
  Wazuh 的密碼不會被寫進 `config.yaml`。
- **暴力破解防護：** 同一 IP 連續登入失敗會觸發暫時鎖定
  （預設：3 次失敗鎖 30 分鐘）。
- **最小權限：** 請建立專用的 Wazuh API 使用者與角色，
  只給維運人員真正需要的權限（見 `create_api_user.py`）。

## 內建的強化措施

本應用程式內建輸入驗證與注入防護，包括：

- 針對代理程式 ID、群組名稱、節點名稱、使用者名稱的嚴格驗證器
- 防止路徑穿越的路徑白名單驗證
- 所有 CLI 呼叫的 shell 參數轉義（`shlex.quote`）
- WPK 上傳的安全檔案處理（`werkzeug.utils.secure_filename`）
- 防止記錄注入的記錄消毒
- 管理操作的操作記錄與稽核軌跡

## 免責聲明

本軟體以「現狀」提供，不附帶任何形式的保證。
進行破壞性操作（刪除、重新啟動、升級）之前，
請先備份重要設定，並在非正式環境中測試。使用風險由使用者自行承擔。
